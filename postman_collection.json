{
  "info": {
    "name": "Alaba Marketplace API",
    "_postman_id": "",
    "description": "Auto-generated Postman collection (best-effort)",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "ADDRESS",
      "item": [
        {
          "name": "GET /address/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/address/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "address",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('addressId', address?.id || address?._id || data?.id || data?.addressId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /address/all/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/address/all/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "address",
                "all",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('addressId', address?.id || address?._id || data?.id || data?.addressId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /address",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/address",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "address"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('addressId', address?.id || address?._id || data?.id || data?.addressId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /address/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/address/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "address",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('addressId', address?.id || address?._id || data?.id || data?.addressId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /address/setdefault/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/address/setdefault/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "address",
                "setdefault",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('addressId', address?.id || address?._id || data?.id || data?.addressId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /address/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/address/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "address",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('addressId', address?.id || address?._id || data?.id || data?.addressId);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "BANNER",
      "item": [
        {
          "name": "GET /banner/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/banner/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "banner",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /banner",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/banner",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "banner"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /banner/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/banner/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "banner",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /banner/position/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/banner/position/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "banner",
                "position",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /banner/status/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/banner/status/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "banner",
                "status",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /banner/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/banner/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "banner",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "BOOST REQUESTS",
      "item": [
        {
          "name": "GET /boost-requests",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/boost-requests",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "boost-requests"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /boost-requests/active",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/boost-requests/active",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "boost-requests",
                "active"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /boost-requests/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/boost-requests/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "boost-requests",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /boost-requests",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/boost-requests",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "boost-requests"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /boost-requests",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/boost-requests",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "boost-requests"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /boost-requests/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/boost-requests/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "boost-requests",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /boost-requests/approve",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/boost-requests/approve",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "boost-requests",
                "approve"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /boost-requests/priority",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/boost-requests/priority",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "boost-requests",
                "priority"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "BUSINESSTYPE",
      "item": [
        {
          "name": "GET /businesstype",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/businesstype",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "businesstype"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /businesstype",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/businesstype",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "businesstype"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /businesstype/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/businesstype/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "businesstype",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /businesstype/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/businesstype/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "businesstype",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "CALCULATE DELIVERY",
      "item": [
        {
          "name": "POST /calculate_delivery",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/calculate_delivery",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "calculate_delivery"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /calculate_delivery/new",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/calculate_delivery/new",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "calculate_delivery",
                "new"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "CART",
      "item": [
        {
          "name": "GET /cart/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/cart/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "cart",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /cart",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/cart",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "cart"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /cart/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/cart/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "cart",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /cart/clear-all",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/cart/clear-all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "cart",
                "clear-all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /cart/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/cart/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "cart",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "CATEGORY",
      "item": [
        {
          "name": "GET /category",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/category",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "category"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('categoryId', category?.id || category?._id || data?.id || data?.categoryId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /category/featured",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/category/featured",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "category",
                "featured"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('categoryId', category?.id || category?._id || data?.id || data?.categoryId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /category/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/category/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "category",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('categoryId', category?.id || category?._id || data?.id || data?.categoryId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /category",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/category",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "category"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('categoryId', category?.id || category?._id || data?.id || data?.categoryId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /category/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/category/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "category",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('categoryId', category?.id || category?._id || data?.id || data?.categoryId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /category/position/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/category/position/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "category",
                "position",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('categoryId', category?.id || category?._id || data?.id || data?.categoryId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /category/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/category/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "category",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('categoryId', category?.id || category?._id || data?.id || data?.categoryId);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "COUNTRIES",
      "item": [
        {
          "name": "GET /countries",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/countries",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "countries"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /countries/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/countries/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "countries",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /countries",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/countries",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "countries"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /countries/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/countries/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "countries",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /countries/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/countries/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "countries",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "DASHBOARD",
      "item": [
        {
          "name": "GET /dashboard/count",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/dashboard/count",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "dashboard",
                "count"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /dashboard/statistics",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/dashboard/statistics",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "dashboard",
                "statistics"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /dashboard/order_statistics",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/dashboard/order_statistics",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "dashboard",
                "order_statistics"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "DELIVERYCHARGE",
      "item": [
        {
          "name": "GET /deliverycharge",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/deliverycharge",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "deliverycharge"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /deliverycharge",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/deliverycharge",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "deliverycharge"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /deliverycharge/upsert",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/deliverycharge/upsert",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "deliverycharge",
                "upsert"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /deliverycharge",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/deliverycharge",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "deliverycharge"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /deliverycharge/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/deliverycharge/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "deliverycharge",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "DELIVERY",
      "item": [
        {
          "name": "POST /delivery/company/register",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/company/register",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "company",
                "register"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /delivery/driver/register",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/driver/register",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "driver",
                "register"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/companies",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/companies",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "companies"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/companies/approved",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/companies/approved",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "companies",
                "approved"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/company/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/company/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "company",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /delivery/company/:id/status",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/company/:id/status",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "company",
                ":id",
                "status"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/drivers",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/drivers",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "drivers"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/driver/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/driver/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "driver",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /delivery/driver/:id/status",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/driver/:id/status",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "driver",
                ":id",
                "status"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /delivery/invitation/send",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/invitation/send",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "invitation",
                "send"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/invitation/driver/:driverId",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/invitation/driver/:driverId",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "invitation",
                "driver",
                ":driverId"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/invitation/company/:companyId",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/invitation/company/:companyId",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "invitation",
                "company",
                ":companyId"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /delivery/invitation/:id/accept",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/invitation/:id/accept",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "invitation",
                ":id",
                "accept"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /delivery/invitation/:id/reject",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/invitation/:id/reject",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "invitation",
                ":id",
                "reject"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/orders/available",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/orders/available",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "orders",
                "available"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/orders/debug",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/orders/debug",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "orders",
                "debug"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /delivery/order/:orderId/accept",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/order/:orderId/accept",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "order",
                ":orderId",
                "accept"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/company/:companyId/drivers",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/company/:companyId/drivers",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "company",
                ":companyId",
                "drivers"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/company/:companyId/drivers/available",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/company/:companyId/drivers/available",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "company",
                ":companyId",
                "drivers",
                "available"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/company/:companyId/orders",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/company/:companyId/orders",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "company",
                ":companyId",
                "orders"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/company/:companyId/orders/accepted",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/company/:companyId/orders/accepted",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "company",
                ":companyId",
                "orders",
                "accepted"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/company/:companyId/orders/accepted-with-drivers",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/company/:companyId/orders/accepted-with-drivers",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "company",
                ":companyId",
                "orders",
                "accepted-with-drivers"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /delivery/order/:orderId/pickup",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/order/:orderId/pickup",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "order",
                ":orderId",
                "pickup"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/driver/:driverId/orders",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/driver/:driverId/orders",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "driver",
                ":driverId",
                "orders"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/driver/:driverId/orders/pending",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/driver/:driverId/orders/pending",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "driver",
                ":driverId",
                "orders",
                "pending"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/driver/:driverId/orders/completed",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/driver/:driverId/orders/completed",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "driver",
                ":driverId",
                "orders",
                "completed"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/driver/:driverId/orders/available",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/driver/:driverId/orders/available",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "driver",
                ":driverId",
                "orders",
                "available"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /delivery/driver/:driverId/order/:orderId/select",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/driver/:driverId/order/:orderId/select",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "driver",
                ":driverId",
                "order",
                ":orderId",
                "select"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /delivery/order/:orderId/deliver",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/order/:orderId/deliver",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "order",
                ":orderId",
                "deliver"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/orders/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/orders/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "orders",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "DISTANCECHARGE",
      "item": [
        {
          "name": "GET /distancecharge",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/distancecharge",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "distancecharge"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /distancecharge",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/distancecharge",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "distancecharge"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /distancecharge/upsert",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/distancecharge/upsert",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "distancecharge",
                "upsert"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /distancecharge",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/distancecharge",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "distancecharge"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /distancecharge/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/distancecharge/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "distancecharge",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "ENQUIRY",
      "item": [
        {
          "name": "GET /Enquiry/get",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/Enquiry/get",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "Enquiry",
                "get"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('enquiryId', enquiry?.id || enquiry?._id || data?.id);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /Enquiry/post",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/Enquiry/post",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "Enquiry",
                "post"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('enquiryId', enquiry?.id || enquiry?._id || data?.id);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /Enquiry/update/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/Enquiry/update/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "Enquiry",
                "update",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('enquiryId', enquiry?.id || enquiry?._id || data?.id);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PATCH /Enquiry/patch/:id",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/Enquiry/patch/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "Enquiry",
                "patch",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('enquiryId', enquiry?.id || enquiry?._id || data?.id);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /Enquiry/delete/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/Enquiry/delete/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "Enquiry",
                "delete",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('enquiryId', enquiry?.id || enquiry?._id || data?.id);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "FEATURED PRODUCTS",
      "item": [
        {
          "name": "GET /featured-products/products",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/featured-products/products",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "featured-products",
                "products"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /featured-products/position/:position",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/featured-products/position/:position",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "featured-products",
                "position",
                ":position"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /featured-products/position/:position/products",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/featured-products/position/:position/products",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "featured-products",
                "position",
                ":position",
                "products"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "GOOGLE PROXY",
      "item": [
        {
          "name": "GET /google-proxy/placepicker",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/google-proxy/placepicker",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "google-proxy",
                "placepicker"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /google-proxy/autocomplete",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/google-proxy/autocomplete",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "google-proxy",
                "autocomplete"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "IMG COMPRESS",
      "item": [
        {
          "name": "POST /img_compress/compress",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/img_compress/compress",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "img_compress",
                "compress"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /img_compress/file",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/img_compress/file",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "img_compress",
                "file"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /img_compress/file/both",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/img_compress/file/both",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "img_compress",
                "file",
                "both"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /img_compress/video",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/img_compress/video",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "img_compress",
                "video"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "INDIVIDUAL SELLER",
      "item": [
        {
          "name": "GET /individual_seller/pgn",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/individual_seller/pgn",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "individual_seller",
                "pgn"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /individual_seller/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/individual_seller/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "individual_seller",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /individual_seller/create",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/individual_seller/create",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "individual_seller",
                "create"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /individual_seller/send_mail",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/individual_seller/send_mail",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "individual_seller",
                "send_mail"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /individual_seller/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/individual_seller/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "individual_seller",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /individual_seller/status/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/individual_seller/status/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "individual_seller",
                "status",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /individual_seller/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/individual_seller/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "individual_seller",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "INVOICE",
      "item": [
        {
          "name": "POST /invoice/create",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/invoice/create",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "invoice",
                "create"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /invoice/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/invoice/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "invoice",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /invoice/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/invoice/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "invoice",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /invoice/get/:token",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/invoice/get/:token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "invoice",
                "get",
                ":token"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /invoice/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/invoice/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "invoice",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /invoice/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/invoice/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "invoice",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "LANDING",
      "item": [
        {
          "name": "GET /landing",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/landing",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "landing"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "NEW ADDRESS",
      "item": [
        {
          "name": "GET /new-address",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/new-address",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "new-address"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /new-address/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/new-address/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "new-address",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /new-address",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/new-address",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "new-address"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /new-address/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/new-address/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "new-address",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /new-address/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/new-address/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "new-address",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "NEW DISTANCE CHARGE",
      "item": [
        {
          "name": "GET /new-distance-charge",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/new-distance-charge",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "new-distance-charge"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /new-distance-charge/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/new-distance-charge/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "new-distance-charge",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /new-distance-charge",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/new-distance-charge",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "new-distance-charge"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /new-distance-charge/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/new-distance-charge/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "new-distance-charge",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /new-distance-charge/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/new-distance-charge/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "new-distance-charge",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "NOTIFICATIONS",
      "item": [
        {
          "name": "GET /notifications/test/:token",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/notifications/test/:token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "notifications",
                "test",
                ":token"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /notifications/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/notifications/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "notifications",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PATCH /notifications/read/:id",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/notifications/read/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "notifications",
                "read",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "OFFERS",
      "item": [
        {
          "name": "GET /offers",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/offers",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "offers"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /offers/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/offers/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "offers",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /offers/auth",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/offers/auth",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "offers",
                "auth"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /offers/:slug",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/offers/:slug",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "offers",
                ":slug"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /offers",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/offers",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "offers"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /offers/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/offers/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "offers",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /offers/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/offers/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "offers",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /offers/offer_product/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/offers/offer_product/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "offers",
                "offer_product",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "ORDER",
      "item": [
        {
          "name": "GET /order/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/all-orders-debug",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/all-orders-debug",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "all-orders-debug"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/store",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/store",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "store"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/getall",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/getall",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "getall"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/buy_again",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/buy_again",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "buy_again"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/store/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/store/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "store",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/user/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/user/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "user",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/details/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/details/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "details",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/get_one/admin/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/get_one/admin/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "get_one",
                "admin",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/get_one/seller/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/get_one/seller/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "get_one",
                "seller",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/get_one/user/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/get_one/user/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "get_one",
                "user",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /order",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /order/update_status/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/update_status/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "update_status",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /order/update_payment/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/update_payment/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "update_payment",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /order/cancel_order/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/cancel_order/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "cancel_order",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "ORDERSTATUS",
      "item": [
        {
          "name": "GET /orderStatus/all/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/orderStatus/all/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "orderStatus",
                "all",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "SUBSTITUTION",
      "item": [
        {
          "name": "GET /substitution/details/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/substitution/details/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "substitution",
                "details",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /substitution/five-mint-check",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/substitution/five-mint-check",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "substitution",
                "five-mint-check"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /substitution/create",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/substitution/create",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "substitution",
                "create"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /substitution/substitute/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/substitution/substitute/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "substitution",
                "substitute",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /substitution/update_order/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/substitution/update_order/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "substitution",
                "update_order",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PAYMENT GATEWAY",
      "item": [
        {
          "name": "GET /payment_gateway/token",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/payment_gateway/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "payment_gateway",
                "token"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /payment_gateway/order",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/payment_gateway/order",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "payment_gateway",
                "order"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /payment_gateway/details",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/payment_gateway/details",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "payment_gateway",
                "details"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /payment_gateway/refund",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/payment_gateway/refund",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "payment_gateway",
                "refund"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PAYMENTLOG",
      "item": [
        {
          "name": "POST /paymentlog",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paymentlog",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paymentlog"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PAYMENT SPLITS",
      "item": [
        {
          "name": "POST /payment-splits/create/:orderId",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/payment-splits/create/:orderId",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "payment-splits",
                "create",
                ":orderId"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /payment-splits/process/:orderId",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/payment-splits/process/:orderId",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "payment-splits",
                "process",
                ":orderId"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /payment-splits/verify/:reference",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/payment-splits/verify/:reference",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "payment-splits",
                "verify",
                ":reference"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /payment-splits/store/my-splits",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/payment-splits/store/my-splits",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "payment-splits",
                "store",
                "my-splits"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /payment-splits/store/:storeId",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/payment-splits/store/:storeId",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "payment-splits",
                "store",
                ":storeId"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /payment-splits/admin/summary",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/payment-splits/admin/summary",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "payment-splits",
                "admin",
                "summary"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PAYSTACK",
      "item": [
        {
          "name": "POST /paystack/initialize",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/initialize",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "initialize"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /paystack/initialize-split",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/initialize-split",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "initialize-split"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /paystack/verify",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/verify",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "verify"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack/verify",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/verify",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "verify"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /paystack/refund",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/refund",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "refund"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /paystack/webhook",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/webhook",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "webhook"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack/public-key",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/public-key",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "public-key"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack/transaction",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/transaction",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "transaction"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack/transactions",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/transactions",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "transactions"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "🧾 Paystack Reconciliation",
          "item": [
            {
              "name": "GET /paystack/reconciliation/orphaned-payments",
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  },
                  {
                    "key": "Authorization",
                    "value": "Bearer {{authToken}}"
                  }
                ],
                "url": {
                  "raw": "{{baseUrl}}/paystack/reconciliation/orphaned-payments",
                  "host": [
                    "{{baseUrl}}"
                  ],
                  "path": [
                    "paystack",
                    "reconciliation",
                    "orphaned-payments"
                  ]
                },
                "description": "Admin-only endpoint for successful online payments that are not linked to valid orders."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "let json = null;",
                      "try {",
                      "  json = pm.response.json();",
                      "} catch (error) {",
                      "  console.log('Response is not valid JSON', error);",
                      "}",
                      "if (json) {",
                      "  const setIf = (key, value) => {",
                      "    if (value !== undefined && value !== null && value !== '') {",
                      "      const normalized = String(value);",
                      "      pm.collectionVariables.set(key, normalized);",
                      "      if (pm.environment) {",
                      "        pm.environment.set(key, normalized);",
                      "      }",
                      "    }",
                      "  };",
                      "  const data = json?.data ?? json;",
                      "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                      "  const results = Array.isArray(data?.results) ? data.results : [];",
                      "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                      "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                      "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                      "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                      "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                      "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                      "  const user = data?.user || first?.user || first;",
                      "  const product = first?.product || data?.product || first;",
                      "  const address = first?.address || data?.address || first;",
                      "  const category = first?.category || data?.category || first;",
                      "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                      "  const enquiry = data?.enquiry || first?.enquiry || first;",
                      "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                      "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                      "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                      "  setIf('paymentReference', paystackReference);",
                      "  setIf('reconcileReference', paystackReference);",
                      "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                      "  setIf('paystackStatus', data?.status || json?.status);",
                      "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                      "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                      "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                      "  pm.test('Status code is 200', function () {",
                      "    pm.response.to.have.status(200);",
                      "  });",
                      "  pm.test('Reconciliation has orphanedCount', function () {",
                      "    pm.expect(data).to.have.property('orphanedCount');",
                      "  });",
                      "}"
                    ]
                  }
                }
              ],
              "response": []
            },
            {
              "name": "GET /paystack/reconciliation/orphaned-guest-checkout",
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  },
                  {
                    "key": "Authorization",
                    "value": "Bearer {{authToken}}"
                  }
                ],
                "url": {
                  "raw": "{{baseUrl}}/paystack/reconciliation/orphaned-guest-checkout",
                  "host": [
                    "{{baseUrl}}"
                  ],
                  "path": [
                    "paystack",
                    "reconciliation",
                    "orphaned-guest-checkout"
                  ]
                },
                "description": "Admin-only endpoint to find guest checkouts marked paid but not completed."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "let json = null;",
                      "try {",
                      "  json = pm.response.json();",
                      "} catch (error) {",
                      "  console.log('Response is not valid JSON', error);",
                      "}",
                      "if (json) {",
                      "  const setIf = (key, value) => {",
                      "    if (value !== undefined && value !== null && value !== '') {",
                      "      const normalized = String(value);",
                      "      pm.collectionVariables.set(key, normalized);",
                      "      if (pm.environment) {",
                      "        pm.environment.set(key, normalized);",
                      "      }",
                      "    }",
                      "  };",
                      "  const data = json?.data ?? json;",
                      "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                      "  const results = Array.isArray(data?.results) ? data.results : [];",
                      "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                      "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                      "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                      "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                      "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                      "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                      "  const user = data?.user || first?.user || first;",
                      "  const product = first?.product || data?.product || first;",
                      "  const address = first?.address || data?.address || first;",
                      "  const category = first?.category || data?.category || first;",
                      "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                      "  const enquiry = data?.enquiry || first?.enquiry || first;",
                      "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                      "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                      "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                      "  setIf('paymentReference', paystackReference);",
                      "  setIf('reconcileReference', paystackReference);",
                      "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                      "  setIf('paystackStatus', data?.status || json?.status);",
                      "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                      "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                      "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                      "  pm.test('Status code is 200', function () {",
                      "    pm.response.to.have.status(200);",
                      "  });",
                      "  pm.test('Guest reconciliation has unprocessedCheckoutCount', function () {",
                      "    pm.expect(data).to.have.property('unprocessedCheckoutCount');",
                      "  });",
                      "}"
                    ]
                  }
                }
              ],
              "response": []
            },
            {
              "name": "GET /paystack/reconciliation/full-report",
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  },
                  {
                    "key": "Authorization",
                    "value": "Bearer {{authToken}}"
                  }
                ],
                "url": {
                  "raw": "{{baseUrl}}/paystack/reconciliation/full-report?page=1&take=50",
                  "host": [
                    "{{baseUrl}}"
                  ],
                  "path": [
                    "paystack",
                    "reconciliation",
                    "full-report"
                  ],
                  "query": [
                    {
                      "key": "page",
                      "value": "1"
                    },
                    {
                      "key": "take",
                      "value": "50"
                    }
                  ]
                },
                "description": "Admin-only endpoint that returns a consolidated paystack reconciliation report. Supports pagination via page and take query params."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "let json = null;",
                      "try {",
                      "  json = pm.response.json();",
                      "} catch (error) {",
                      "  console.log('Response is not valid JSON', error);",
                      "}",
                      "if (json) {",
                      "  const setIf = (key, value) => {",
                      "    if (value !== undefined && value !== null && value !== '') {",
                      "      const normalized = String(value);",
                      "      pm.collectionVariables.set(key, normalized);",
                      "      if (pm.environment) {",
                      "        pm.environment.set(key, normalized);",
                      "      }",
                      "    }",
                      "  };",
                      "  const data = json?.data ?? json;",
                      "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                      "  const results = Array.isArray(data?.results) ? data.results : [];",
                      "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                      "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                      "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                      "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                      "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                      "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                      "  const user = data?.user || first?.user || first;",
                      "  const product = first?.product || data?.product || first;",
                      "  const address = first?.address || data?.address || first;",
                      "  const category = first?.category || data?.category || first;",
                      "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                      "  const enquiry = data?.enquiry || first?.enquiry || first;",
                      "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                      "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                      "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                      "  setIf('paymentReference', paystackReference);",
                      "  setIf('reconcileReference', paystackReference);",
                      "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                      "  setIf('paystackStatus', data?.status || json?.status);",
                      "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                      "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                      "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                      "  pm.test('Status code is 200', function () {",
                      "    pm.response.to.have.status(200);",
                      "  });",
                      "  pm.test('Full report has summary', function () {",
                      "    pm.expect(data).to.have.property('summary');",
                      "  });",
                      "  pm.test('Full report has orderDetails', function () {",
                      "    pm.expect(data).to.have.property('details');",
                      "    pm.expect(data.details).to.have.property('orderDetails');",
                      "  });",
                      "}"
                    ]
                  }
                }
              ],
              "response": []
            },
            {
              "name": "GET /paystack/reconciliation/audit-mismatches",
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  },
                  {
                    "key": "Authorization",
                    "value": "Bearer {{authToken}}"
                  }
                ],
                "url": {
                  "raw": "{{baseUrl}}/paystack/reconciliation/audit-mismatches",
                  "host": [
                    "{{baseUrl}}"
                  ],
                  "path": [
                    "paystack",
                    "reconciliation",
                    "audit-mismatches"
                  ]
                },
                "description": "Admin-only endpoint to audit mismatches between ORDER and ORDER_PAYMENTS records."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "let json = null;",
                      "try {",
                      "  json = pm.response.json();",
                      "} catch (error) {",
                      "  console.log('Response is not valid JSON', error);",
                      "}",
                      "if (json) {",
                      "  const setIf = (key, value) => {",
                      "    if (value !== undefined && value !== null && value !== '') {",
                      "      const normalized = String(value);",
                      "      pm.collectionVariables.set(key, normalized);",
                      "      if (pm.environment) {",
                      "        pm.environment.set(key, normalized);",
                      "      }",
                      "    }",
                      "  };",
                      "  const data = json?.data ?? json;",
                      "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                      "  const results = Array.isArray(data?.results) ? data.results : [];",
                      "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                      "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                      "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                      "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                      "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                      "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                      "  const user = data?.user || first?.user || first;",
                      "  const product = first?.product || data?.product || first;",
                      "  const address = first?.address || data?.address || first;",
                      "  const category = first?.category || data?.category || first;",
                      "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                      "  const enquiry = data?.enquiry || first?.enquiry || first;",
                      "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                      "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                      "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                      "  setIf('paymentReference', paystackReference);",
                      "  setIf('reconcileReference', paystackReference);",
                      "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                      "  setIf('paystackStatus', data?.status || json?.status);",
                      "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                      "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                      "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                      "  pm.test('Status code is 200', function () {",
                      "    pm.response.to.have.status(200);",
                      "  });",
                      "  pm.test('Audit response has summary', function () {",
                      "    pm.expect(data).to.have.property('summary');",
                      "  });",
                      "}"
                    ]
                  }
                }
              ],
              "response": []
            }
          ]
        },
        {
          "name": "GET /paystack/success",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/success",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "success"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack/cancel",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/cancel",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "cancel"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack/failed",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/failed",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "failed"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /paystack/transactions/reconcile",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"dryRun\": true,\n  \"status\": \"success\",\n  \"page\": 1,\n  \"perPage\": 50,\n  \"maxPages\": 1\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/paystack/transactions/reconcile",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "transactions",
                "reconcile"
              ]
            },
            "description": "Admin-only reconciliation for historical Paystack transactions. Run with dryRun=true first, then set dryRun=false to replay reconcilable entries through the existing webhook sync flow."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('paymentReference', reconcile?.reference);",
                  "  setIf('reconcileReference', reconcile?.reference);",
                  "  setIf('paystackStatus', reconcile?.paystack_status);",
                  "  setIf('paystackCustomerEmail', reconcile?.customer_email);",
                  "  setIf('paystackAmountKobo', reconcile?.amount_kobo);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PAYSTACK SUBACCOUNTS",
      "item": [
        {
          "name": "POST /paystack-subaccounts/request",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack-subaccounts/request",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack-subaccounts",
                "request"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack-subaccounts/my-subaccount",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack-subaccounts/my-subaccount",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack-subaccounts",
                "my-subaccount"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack-subaccounts/pending",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack-subaccounts/pending",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack-subaccounts",
                "pending"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /paystack-subaccounts/:id/approve",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack-subaccounts/:id/approve",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack-subaccounts",
                ":id",
                "approve"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /paystack-subaccounts/:id/reject",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack-subaccounts/:id/reject",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack-subaccounts",
                ":id",
                "reject"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack-subaccounts/banks/supported",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack-subaccounts/banks/supported",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack-subaccounts",
                "banks",
                "supported"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack-subaccounts/banks/validate",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack-subaccounts/banks/validate",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack-subaccounts",
                "banks",
                "validate"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack-subaccounts/banks/:bankCode/supported",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack-subaccounts/banks/:bankCode/supported",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack-subaccounts",
                "banks",
                ":bankCode",
                "supported"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack-subaccounts/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack-subaccounts/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack-subaccounts",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PRINT",
      "item": [
        {
          "name": "GET /print/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /print/store",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/store",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "store"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /print/getall",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/getall",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "getall"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /print/buy_again",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/buy_again",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "buy_again"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /print/store/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/store/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "store",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /print/user/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/user/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "user",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /print/details/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/details/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "details",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /print/get_one/admin/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/get_one/admin/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "get_one",
                "admin",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /print/get_one/seller/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/get_one/seller/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "get_one",
                "seller",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /print/get_one/user/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/get_one/user/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "get_one",
                "user",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /print",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /print/update_status/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/update_status/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "update_status",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /print/update_payment/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/update_payment/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "update_payment",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /print/cancel_order/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/cancel_order/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "cancel_order",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PRINT CONFIGERATION",
      "item": [
        {
          "name": "POST /Print_Configeration/bulk",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/Print_Configeration/bulk",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "Print_Configeration",
                "bulk"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /Print_Configeration",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/Print_Configeration",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "Print_Configeration"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /Print_Configeration/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/Print_Configeration/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "Print_Configeration",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /Print_Configeration/price",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/Print_Configeration/price",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "Print_Configeration",
                "price"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /Print_Configeration/configurations",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/Print_Configeration/configurations",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "Print_Configeration",
                "configurations"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /Print_Configeration/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/Print_Configeration/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "Print_Configeration",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PRINTSTATUS",
      "item": [
        {
          "name": "GET /printStatus/all/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/printStatus/all/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "printStatus",
                "all",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PRODUCTS",
      "item": [
        {
          "name": "GET /products/bystore",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/bystore",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "bystore"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /products/bystore/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/bystore/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "bystore",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /products/seller/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/seller/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "seller",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /products/create",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/create",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "create"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /products/upload/",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/upload",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "upload"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /products/upload/files",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/upload/files",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "upload",
                "files"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /products/update/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/update/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "update",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /products/update_image/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/update_image/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "update_image",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /products/update_cover_img/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/update_cover_img/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "update_cover_img",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /products/update_status/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/update_status/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "update_status",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /products/update/variant/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/update/variant/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "update",
                "variant",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /products/delete/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/delete/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "delete",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /products/delete_image/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/delete_image/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "delete_image",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /products/check-same-store",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/check-same-store",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "check-same-store"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PRODUCTIMAGE",
      "item": [
        {
          "name": "PUT /productimage/update/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/productimage/update/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "productimage",
                "update",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PRODUCTSREVIEWS",
      "item": [
        {
          "name": "GET /productsReviews/review",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/productsReviews/review",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "productsReviews",
                "review"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /productsReviews",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/productsReviews",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "productsReviews"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /productsReviews/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/productsReviews/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "productsReviews",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PRODUCT SEARCH",
      "item": [
        {
          "name": "GET /product_search/multi",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/product_search/multi",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "product_search",
                "multi"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /product_search/single",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/product_search/single",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "product_search",
                "single"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /product_search/store_top",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/product_search/store_top",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "product_search",
                "store_top"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /product_search/print/available",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/product_search/print/available",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "product_search",
                "print",
                "available"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /product_search/details/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/product_search/details/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "product_search",
                "details",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /product_search/autocomplete",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/product_search/autocomplete",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "product_search",
                "autocomplete"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /product_search/store/:slug",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/product_search/store/:slug",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "product_search",
                "store",
                ":slug"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /product_search/store_items/:slug",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/product_search/store_items/:slug",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "product_search",
                "store_items",
                ":slug"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /product_search/store_items/category/products",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/product_search/store_items/category/products",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "product_search",
                "store_items",
                "category",
                "products"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /product_search/boosted-category",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/product_search/boosted-category",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "product_search",
                "boosted-category"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PRODUCTVARIANT",
      "item": [
        {
          "name": "POST /productvariant/add_variants",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/productvariant/add_variants",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "productvariant",
                "add_variants"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /productvariant/delete/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/productvariant/delete/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "productvariant",
                "delete",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REFUND REQUEST",
      "item": [
        {
          "name": "GET /refund_request/admin/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/refund_request/admin/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "refund_request",
                "admin",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /refund_request/store/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/refund_request/store/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "refund_request",
                "store",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /refund_request/user/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/refund_request/user/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "refund_request",
                "user",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /refund_request/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/refund_request/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "refund_request",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /refund_request/admin/approve/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/refund_request/admin/approve/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "refund_request",
                "admin",
                "approve",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "SETTINGS",
      "item": [
        {
          "name": "GET /settings",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settings",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settings"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /settings/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settings/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settings",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "SETTLEMENTS",
      "item": [
        {
          "name": "GET /settlements/summary",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/summary",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "summary"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /settlements/history",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/history",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "history"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /settlements/historyById/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/historyById/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "historyById",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /settlements/allSettlementsByStore",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/allSettlementsByStore",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "allSettlementsByStore"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /settlements/allSettlementsByAdmin",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/allSettlementsByAdmin",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "allSettlementsByAdmin"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /settlements/summary/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/summary/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "summary",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /settlements/history/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/history/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "history",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /settlements/details/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/details/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "details",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /settlements/store/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/store/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "store",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /settlements/update/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/update/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "update",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /settlements/updateStatus/:id/:type",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/updateStatus/:id/:type",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "updateStatus",
                ":id",
                ":type"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /settlements/create",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/create",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "create"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /settlements/createByStore",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/createByStore",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "createByStore"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "STATES",
      "item": [
        {
          "name": "GET /states",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/states",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "states"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /states",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/states",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "states"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /states/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/states/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "states",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /states/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/states/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "states",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "COORPORATE STORE",
      "item": [
        {
          "name": "GET /coorporate_store/store_check",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/store_check",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "store_check"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /coorporate_store/account/details",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/account/details",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "account",
                "details"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /coorporate_store/account/details",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/account/details",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "account",
                "details"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /coorporate_store/details",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/details",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "details"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /coorporate_store/dashboard/admin",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/dashboard/admin",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "dashboard",
                "admin"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /coorporate_store/sellerdetails/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/sellerdetails/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "sellerdetails",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /coorporate_store/pgn/:type",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/pgn/:type",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "pgn",
                ":type"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /coorporate_store/dashboard/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/dashboard/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "dashboard",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /coorporate_store/print/time/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/print/time/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "print",
                "time",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /coorporate_store/create",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/create",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "create"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /coorporate_store/createexist",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/createexist",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "createexist"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /coorporate_store/send_mail",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/send_mail",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "send_mail"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /coorporate_store/update",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/update",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "update"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /coorporate_store/status/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/status/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "status",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /coorporate_store/deactivate/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/deactivate/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "deactivate",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "STOREREVIEW",
      "item": [
        {
          "name": "POST /storereview/create",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/storereview/create",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "storereview",
                "create"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "STORE SEARCH",
      "item": [
        {
          "name": "GET /store_search/info",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/store_search/info",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "store_search",
                "info"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /store_search/info/:slug",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/store_search/info/:slug",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "store_search",
                "info",
                ":slug"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /store_search/info/category/store",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/store_search/info/category/store",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "store_search",
                "info",
                "category",
                "store"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /store_search/banner/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/store_search/banner/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "store_search",
                "banner",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "SUBSCRIPTION PLANS",
      "item": [
        {
          "name": "GET /subscription-plans",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subscription-plans",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subscription-plans"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /subscription-plans/active",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subscription-plans/active",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subscription-plans",
                "active"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /subscription-plans/public/active",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subscription-plans/public/active",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subscription-plans",
                "public",
                "active"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /subscription-plans/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subscription-plans/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subscription-plans",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /subscription-plans",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subscription-plans",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subscription-plans"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /subscription-plans",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subscription-plans",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subscription-plans"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /subscription-plans/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subscription-plans/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subscription-plans",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /subscription-plans/bulk-delete",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subscription-plans/bulk-delete",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subscription-plans",
                "bulk-delete"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /subscription-plans/featured-position",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subscription-plans/featured-position",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subscription-plans",
                "featured-position"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "SUBTITUTION TOKEN",
      "item": [
        {
          "name": "POST /subtitution/token",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subtitution/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subtitution",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "SUBCATEGORY",
      "item": [
        {
          "name": "GET /subCategory/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subCategory/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subCategory",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /subCategory",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subCategory",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subCategory"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /subCategory/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subCategory/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subCategory",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /subCategory/position/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subCategory/position/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subCategory",
                "position",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /subCategory/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subCategory/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subCategory",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "USER",
      "item": [
        {
          "name": "GET /user",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /user/refresh-user",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user/refresh-user",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user",
                "refresh-user"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /user/details/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user/details/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user",
                "details",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /user/check_user/validate",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user/check_user/validate",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user",
                "check_user",
                "validate"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /user/deactivate",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user/deactivate",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user",
                "deactivate"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /user/update-name",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user/update-name",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user",
                "update-name"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /user/update-password",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user/update-password",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user",
                "update-password"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /user/add-password",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user/add-password",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user",
                "add-password"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /user/update-email",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user/update-email",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user",
                "update-email"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /user/update-photo",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user/update-photo",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user",
                "update-photo"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /user/update-Phone",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user/update-Phone",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user",
                "update-Phone"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /user/reactivate/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user/reactivate/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user",
                "reactivate",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "AUTH",
      "item": [
        {
          "name": "GET /auth/signout",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/signout",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "signout"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /auth/signoutall",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/signoutall",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "signoutall"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /auth/checkphone/:phone",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/checkphone/:phone",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "checkphone",
                ":phone"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /auth/checkEmail/:email",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/checkEmail/:email",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "checkEmail",
                ":email"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/signup",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/signup",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "signup"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/email-verify",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/email-verify",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "email-verify"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/verify-email",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/verify-email",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "verify-email"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/refresh-token",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/refresh-token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "refresh-token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/phone-login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/phone-login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "phone-login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/google-login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/google-login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "google-login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/apple-login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/apple-login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "apple-login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/forgot-password",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/forgot-password",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "forgot-password"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/reset-password",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/reset-password",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "reset-password"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/request-deactivate",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/request-deactivate",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "request-deactivate"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/deactivate-account",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/deactivate-account",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "deactivate-account"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "USER BANK ACCOUNT",
      "item": [
        {
          "name": "GET /user-bank-account/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user-bank-account/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user-bank-account",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /user-bank-account/all/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user-bank-account/all/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user-bank-account",
                "all",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /user-bank-account",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user-bank-account",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user-bank-account"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /user-bank-account/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user-bank-account/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user-bank-account",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /user-bank-account/soft/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user-bank-account/soft/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user-bank-account",
                "soft",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /user-bank-account",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user-bank-account",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user-bank-account"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /user-bank-account/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user-bank-account/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user-bank-account",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /user-bank-account/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user-bank-account/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user-bank-account",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /user-bank-account/setdefault/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user-bank-account/setdefault/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user-bank-account",
                "setdefault",
                ":id"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /user-bank-account/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user-bank-account/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user-bank-account",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "USERHISTORY",
      "item": [
        {
          "name": "GET /userhistory/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/userhistory/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "userhistory",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "WISHLIST",
      "item": [
        {
          "name": "GET /wishlist/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/wishlist/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "wishlist",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /wishlist",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/wishlist",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "wishlist"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /wishlist/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/wishlist/:id",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "wishlist",
                ":id"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    }
  ],
  "variable": [
    {
      "key": "baseUrl",
      "value": "http://localhost:3000",
      "type": "string"
    },
    {
      "key": "authToken",
      "value": "",
      "type": "string"
    },
    {
      "id": "refreshToken",
      "key": "refreshToken",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "userId",
      "key": "userId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "managedUserId",
      "key": "managedUserId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "userEmail",
      "key": "userEmail",
      "type": "string",
      "value": "olagiddz@gmail.com",
      "enabled": true
    },
    {
      "id": "activeRole",
      "key": "activeRole",
      "type": "string",
      "value": "user",
      "enabled": true
    },
    {
      "id": "storeId",
      "key": "storeId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "orderId",
      "key": "orderId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "productId",
      "key": "productId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "variantId",
      "key": "variantId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "addressId",
      "key": "addressId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "deliveryToken",
      "key": "deliveryToken",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "paymentReference",
      "key": "paymentReference",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "reconcileReference",
      "key": "reconcileReference",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "guestEmail",
      "key": "guestEmail",
      "type": "string",
      "value": "olagiddz@gmail.com",
      "enabled": true
    },
    {
      "id": "paystackStatus",
      "key": "paystackStatus",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "paystackCustomerEmail",
      "key": "paystackCustomerEmail",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "paystackAuthUrl",
      "key": "paystackAuthUrl",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "paystackAmountKobo",
      "key": "paystackAmountKobo",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "enquiryId",
      "key": "enquiryId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "paystackSignature",
      "key": "paystackSignature",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "orderPublicId",
      "key": "orderPublicId",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "categoryId",
      "key": "categoryId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "subCategoryId",
      "key": "subCategoryId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "storeSlug",
      "key": "storeSlug",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "productPid",
      "key": "productPid",
      "type": "string",
      "value": "",
      "enabled": true
    }
  ]
}