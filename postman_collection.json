{
  "info": {
    "name": "Alaba Marketplace API",
    "_postman_id": "",
    "description": "Auto-generated Postman collection (best-effort)",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "PURCHASE FLOW",
      "item": [
        {
          "name": "01 Login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"{{userEmail}}\",\n  \"password\": \"{{password}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "02 Current User /me",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user",
                "me"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "03 Create Address",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/new-address",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "new-address"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"address_type\": \"home\",\n  \"full_address\": \"12 Test Street, Ikeja, Lagos\",\n  \"pincode\": \"100001\",\n  \"phone_no\": \"08000000000\",\n  \"country_id\": {{countryId}},\n  \"state_id\": {{stateId}}\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const addressRoot = data?.address || data;",
                  "  const addressRows = Array.isArray(addressRoot?.rows) ? addressRoot.rows : [];",
                  "  const addressResults = Array.isArray(addressRoot?.results) ? addressRoot.results : [];",
                  "  const addressNested = Array.isArray(addressRoot?.data) ? addressRoot.data : [];",
                  "  const addressList = Array.isArray(addressRoot) ? addressRoot : (addressRows.length ? addressRows : (addressResults.length ? addressResults : addressNested));",
                  "  const selectedAddress = addressList[0] || addressRoot || address || {};",
                  "  const previousAddressId = pm.collectionVariables.get('addressId') || (pm.environment && pm.environment.get('addressId'));",
                  "  const nextAddressId = selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId;",
                  "  if (nextAddressId && previousAddressId && String(nextAddressId) !== String(previousAddressId)) {",
                  "    unsetVar('deliveryToken');",
                  "    unsetVar('deliveryTokenAddressId');",
                  "    unsetVar('deliveryCharge');",
                  "    unsetVar('deliveryDiscount');",
                  "  }",
                  "  setIf('addressId', selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId);",
                  "  setIf('countryId', selectedAddress?.country_id || selectedAddress?.countryId || data?.country_id || data?.countryId);",
                  "  setIf('stateId', selectedAddress?.state_id || selectedAddress?.stateId || data?.state_id || data?.stateId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "04 Add Product To Cart",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/cart",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "cart"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"productId\": {{productId}},\n  \"variantId\": {{variantId}},\n  \"quantity\": {{orderQuantity}}\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const cartRoot = data?.cart || data?.item || data;",
                  "  const cartRows = Array.isArray(cartRoot?.rows) ? cartRoot.rows : [];",
                  "  const cartResults = Array.isArray(cartRoot?.results) ? cartRoot.results : [];",
                  "  const cartNested = Array.isArray(cartRoot?.data) ? cartRoot.data : [];",
                  "  const cartList = Array.isArray(cartRoot) ? cartRoot : (cartRows.length ? cartRows : (cartResults.length ? cartResults : cartNested));",
                  "  const cartItem = cartList[0] || cartRoot || first || {};",
                  "  const cartProduct = cartItem?.product || cartItem?.productDetails || product || {};",
                  "  const cartStore = cartItem?.store || cartItem?.storeDetails || store || {};",
                  "  const cartVariant = cartItem?.variant || cartItem?.variantDetails || firstVariant || {};",
                  "  const cartQuantity = cartItem?.quantity || cartItem?.qty || pm.collectionVariables.get('orderQuantity') || 1;",
                  "  const cartPrice = cartItem?.price || cartItem?.buyPrice || cartItem?.unit_price || cartItem?.unitPrice || cartProduct?.price || cartProduct?.retail_rate || (cartItem?.totalPrice && cartQuantity ? Number(cartItem.totalPrice) / Number(cartQuantity) : undefined);",
                  "  const cartWeight = cartItem?.productWeight || cartItem?.product_weight || cartItem?.weight || cartProduct?.product_weight || cartProduct?.productWeight || cartProduct?.weight || pm.collectionVariables.get('productWeight') || 1;",
                  "  setIf('cartItemId', cartItem?.id || cartItem?._id || cartItem?.cartId || cartItem?.cart_id);",
                  "  setIf('productId', cartItem?.productId || cartItem?.product_id || cartProduct?._id || cartProduct?.id || cartProduct?.productId);",
                  "  setIf('productPid', cartItem?.pid || cartProduct?.pid);",
                  "  setIf('variantId', cartItem?.variantId ?? cartItem?.variant_id ?? cartVariant?.id ?? cartVariant?._id ?? 'null');",
                  "  setIf('storeId', cartItem?.storeId || cartItem?.store_id || cartProduct?.store_id || cartProduct?.storeId || cartStore?.id || cartStore?._id);",
                  "  setIf('orderQuantity', cartQuantity);",
                  "  setIf('productPrice', cartPrice);",
                  "  setIf('productName', cartItem?.name || cartProduct?.name || cartProduct?.title);",
                  "  setIf('productImage', cartItem?.image || cartProduct?.image || cartVariant?.image);",
                  "  setIf('productWeight', cartWeight);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "05 Get Cart",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/cart/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "cart",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const cartRoot = data?.cart || data?.item || data;",
                  "  const cartRows = Array.isArray(cartRoot?.rows) ? cartRoot.rows : [];",
                  "  const cartResults = Array.isArray(cartRoot?.results) ? cartRoot.results : [];",
                  "  const cartNested = Array.isArray(cartRoot?.data) ? cartRoot.data : [];",
                  "  const cartList = Array.isArray(cartRoot) ? cartRoot : (cartRows.length ? cartRows : (cartResults.length ? cartResults : cartNested));",
                  "  const cartItem = cartList[0] || cartRoot || first || {};",
                  "  const cartProduct = cartItem?.product || cartItem?.productDetails || product || {};",
                  "  const cartStore = cartItem?.store || cartItem?.storeDetails || store || {};",
                  "  const cartVariant = cartItem?.variant || cartItem?.variantDetails || firstVariant || {};",
                  "  const cartQuantity = cartItem?.quantity || cartItem?.qty || pm.collectionVariables.get('orderQuantity') || 1;",
                  "  const cartPrice = cartItem?.price || cartItem?.buyPrice || cartItem?.unit_price || cartItem?.unitPrice || cartProduct?.price || cartProduct?.retail_rate || (cartItem?.totalPrice && cartQuantity ? Number(cartItem.totalPrice) / Number(cartQuantity) : undefined);",
                  "  const cartWeight = cartItem?.productWeight || cartItem?.product_weight || cartItem?.weight || cartProduct?.product_weight || cartProduct?.productWeight || cartProduct?.weight || pm.collectionVariables.get('productWeight') || 1;",
                  "  setIf('cartItemId', cartItem?.id || cartItem?._id || cartItem?.cartId || cartItem?.cart_id);",
                  "  setIf('productId', cartItem?.productId || cartItem?.product_id || cartProduct?._id || cartProduct?.id || cartProduct?.productId);",
                  "  setIf('productPid', cartItem?.pid || cartProduct?.pid);",
                  "  setIf('variantId', cartItem?.variantId ?? cartItem?.variant_id ?? cartVariant?.id ?? cartVariant?._id ?? 'null');",
                  "  setIf('storeId', cartItem?.storeId || cartItem?.store_id || cartProduct?.store_id || cartProduct?.storeId || cartStore?.id || cartStore?._id);",
                  "  setIf('orderQuantity', cartQuantity);",
                  "  setIf('productPrice', cartPrice);",
                  "  setIf('productName', cartItem?.name || cartProduct?.name || cartProduct?.title);",
                  "  setIf('productImage', cartItem?.image || cartProduct?.image || cartVariant?.image);",
                  "  setIf('productWeight', cartWeight);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "06 Calculate Delivery Token",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/calculate_delivery/new",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "calculate_delivery",
                "new"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"cart\": [\n    {\n      \"weight\": {{productWeight}},\n      \"quantity\": {{orderQuantity}}\n    }\n  ],\n  \"address\": {\n    \"id\": {{addressId}},\n    \"country_id\": {{countryId}},\n    \"state_id\": {{stateId}}\n  }\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const deliveryToken = json?.token || data?.token;",
                  "  const deliveryTokenPayload = decodeJwtPayload(deliveryToken);",
                  "  setIf('deliveryToken', deliveryToken);",
                  "  setIf('deliveryTokenAddressId', deliveryTokenPayload?.data?.addressId || pm.collectionVariables.get('addressId'));",
                  "  setIf('deliveryCharge', data?.amount || data?.delivery_charge || data?.deliveryCharge);",
                  "  setIf('deliveryDiscount', data?.discount);",
                  "  setIf('productWeight', data?.totalWeight || data?.total_weight || pm.collectionVariables.get('productWeight'));",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "07 Initialize Order Paystack Checkout",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"cart\": [\n    {\n      \"id\": {{cartItemId}},\n      \"productId\": {{productId}},\n      \"variantId\": {{variantId}},\n      \"storeId\": {{storeId}},\n      \"quantity\": {{orderQuantity}}\n    }\n  ],\n  \"payment\": {\n    \"type\": \"paystack\",\n    \"callback_url\": \"{{callbackUrl}}\"\n  },\n  \"address\": {\n    \"id\": {{addressId}}\n  },\n  \"charges\": {\n    \"token\": \"{{deliveryToken}}\"\n  }\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            },
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const addressId = pm.variables.get('addressId');",
                  "const deliveryToken = pm.variables.get('deliveryToken');",
                  "let tokenAddressId = pm.variables.get('deliveryTokenAddressId');",
                  "",
                  "const decodeJwtPayload = (token) => {",
                  "  try {",
                  "    if (!token || String(token).split('.').length < 2) return null;",
                  "    let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "    while (payload.length % 4) payload += '=';",
                  "    return JSON.parse(atob(payload));",
                  "  } catch (error) {",
                  "    console.log('Unable to decode delivery token', error);",
                  "    return null;",
                  "  }",
                  "};",
                  "",
                  "if (!deliveryToken) {",
                  "  throw new Error('Missing deliveryToken. Run POST /calculate_delivery/new after selecting address and cart, then retry /order.');",
                  "}",
                  "",
                  "if (!tokenAddressId) {",
                  "  const decoded = decodeJwtPayload(deliveryToken);",
                  "  tokenAddressId = decoded?.data?.addressId;",
                  "  if (tokenAddressId !== undefined && tokenAddressId !== null) {",
                  "    pm.collectionVariables.set('deliveryTokenAddressId', String(tokenAddressId));",
                  "    if (pm.environment) pm.environment.set('deliveryTokenAddressId', String(tokenAddressId));",
                  "  }",
                  "}",
                  "",
                  "if (addressId && tokenAddressId && String(addressId) !== String(tokenAddressId)) {",
                  "  throw new Error(`Stale deliveryToken: token addressId=${tokenAddressId}, order addressId=${addressId}. Run POST /calculate_delivery/new again, then retry /order.`);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "08 Verify Paystack Payment",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/verify",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "verify"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"reference\": \"{{paymentReference}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "09 Get My Orders",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/me?page=1&take=10&status=&sort=&order=DESC",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "me?page=1&take=10&status=&sort=&order=DESC"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "ADDRESS",
      "item": [
        {
          "name": "GET /address/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/address/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "address",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const addressRoot = data?.address || data;",
                  "  const addressRows = Array.isArray(addressRoot?.rows) ? addressRoot.rows : [];",
                  "  const addressResults = Array.isArray(addressRoot?.results) ? addressRoot.results : [];",
                  "  const addressNested = Array.isArray(addressRoot?.data) ? addressRoot.data : [];",
                  "  const addressList = Array.isArray(addressRoot) ? addressRoot : (addressRows.length ? addressRows : (addressResults.length ? addressResults : addressNested));",
                  "  const selectedAddress = addressList[0] || addressRoot || address || {};",
                  "  const previousAddressId = pm.collectionVariables.get('addressId') || (pm.environment && pm.environment.get('addressId'));",
                  "  const nextAddressId = selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId;",
                  "  if (nextAddressId && previousAddressId && String(nextAddressId) !== String(previousAddressId)) {",
                  "    unsetVar('deliveryToken');",
                  "    unsetVar('deliveryTokenAddressId');",
                  "    unsetVar('deliveryCharge');",
                  "    unsetVar('deliveryDiscount');",
                  "  }",
                  "  setIf('addressId', selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId);",
                  "  setIf('countryId', selectedAddress?.country_id || selectedAddress?.countryId || data?.country_id || data?.countryId);",
                  "  setIf('stateId', selectedAddress?.state_id || selectedAddress?.stateId || data?.state_id || data?.stateId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /address/all/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/address/all/{{addressId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "address",
                "all",
                "{{addressId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const addressRoot = data?.address || data;",
                  "  const addressRows = Array.isArray(addressRoot?.rows) ? addressRoot.rows : [];",
                  "  const addressResults = Array.isArray(addressRoot?.results) ? addressRoot.results : [];",
                  "  const addressNested = Array.isArray(addressRoot?.data) ? addressRoot.data : [];",
                  "  const addressList = Array.isArray(addressRoot) ? addressRoot : (addressRows.length ? addressRows : (addressResults.length ? addressResults : addressNested));",
                  "  const selectedAddress = addressList[0] || addressRoot || address || {};",
                  "  const previousAddressId = pm.collectionVariables.get('addressId') || (pm.environment && pm.environment.get('addressId'));",
                  "  const nextAddressId = selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId;",
                  "  if (nextAddressId && previousAddressId && String(nextAddressId) !== String(previousAddressId)) {",
                  "    unsetVar('deliveryToken');",
                  "    unsetVar('deliveryTokenAddressId');",
                  "    unsetVar('deliveryCharge');",
                  "    unsetVar('deliveryDiscount');",
                  "  }",
                  "  setIf('addressId', selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId);",
                  "  setIf('countryId', selectedAddress?.country_id || selectedAddress?.countryId || data?.country_id || data?.countryId);",
                  "  setIf('stateId', selectedAddress?.state_id || selectedAddress?.stateId || data?.state_id || data?.stateId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /address/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/address/{{addressId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "address",
                "{{addressId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const addressRoot = data?.address || data;",
                  "  const addressRows = Array.isArray(addressRoot?.rows) ? addressRoot.rows : [];",
                  "  const addressResults = Array.isArray(addressRoot?.results) ? addressRoot.results : [];",
                  "  const addressNested = Array.isArray(addressRoot?.data) ? addressRoot.data : [];",
                  "  const addressList = Array.isArray(addressRoot) ? addressRoot : (addressRows.length ? addressRows : (addressResults.length ? addressResults : addressNested));",
                  "  const selectedAddress = addressList[0] || addressRoot || address || {};",
                  "  const previousAddressId = pm.collectionVariables.get('addressId') || (pm.environment && pm.environment.get('addressId'));",
                  "  const nextAddressId = selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId;",
                  "  if (nextAddressId && previousAddressId && String(nextAddressId) !== String(previousAddressId)) {",
                  "    unsetVar('deliveryToken');",
                  "    unsetVar('deliveryTokenAddressId');",
                  "    unsetVar('deliveryCharge');",
                  "    unsetVar('deliveryDiscount');",
                  "  }",
                  "  setIf('addressId', selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId);",
                  "  setIf('countryId', selectedAddress?.country_id || selectedAddress?.countryId || data?.country_id || data?.countryId);",
                  "  setIf('stateId', selectedAddress?.state_id || selectedAddress?.stateId || data?.state_id || data?.stateId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /address",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/address",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "address"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"fullname\": \"John Doe\",\n  \"phone\": \"+2348012345678\",\n  \"street\": \"12 Example St\",\n  \"city\": \"Lagos\",\n  \"state\": \"Lagos\",\n  \"country\": \"Nigeria\",\n  \"postalCode\": \"100001\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const addressRoot = data?.address || data;",
                  "  const addressRows = Array.isArray(addressRoot?.rows) ? addressRoot.rows : [];",
                  "  const addressResults = Array.isArray(addressRoot?.results) ? addressRoot.results : [];",
                  "  const addressNested = Array.isArray(addressRoot?.data) ? addressRoot.data : [];",
                  "  const addressList = Array.isArray(addressRoot) ? addressRoot : (addressRows.length ? addressRows : (addressResults.length ? addressResults : addressNested));",
                  "  const selectedAddress = addressList[0] || addressRoot || address || {};",
                  "  const previousAddressId = pm.collectionVariables.get('addressId') || (pm.environment && pm.environment.get('addressId'));",
                  "  const nextAddressId = selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId;",
                  "  if (nextAddressId && previousAddressId && String(nextAddressId) !== String(previousAddressId)) {",
                  "    unsetVar('deliveryToken');",
                  "    unsetVar('deliveryTokenAddressId');",
                  "    unsetVar('deliveryCharge');",
                  "    unsetVar('deliveryDiscount');",
                  "  }",
                  "  setIf('addressId', selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId);",
                  "  setIf('countryId', selectedAddress?.country_id || selectedAddress?.countryId || data?.country_id || data?.countryId);",
                  "  setIf('stateId', selectedAddress?.state_id || selectedAddress?.stateId || data?.state_id || data?.stateId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /address/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/address/{{addressId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "address",
                "{{addressId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"fullname\": \"John Doe\",\n  \"phone\": \"+2348012345678\",\n  \"street\": \"12 Example St\",\n  \"city\": \"Lagos\",\n  \"state\": \"Lagos\",\n  \"country\": \"Nigeria\",\n  \"postalCode\": \"100001\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const addressRoot = data?.address || data;",
                  "  const addressRows = Array.isArray(addressRoot?.rows) ? addressRoot.rows : [];",
                  "  const addressResults = Array.isArray(addressRoot?.results) ? addressRoot.results : [];",
                  "  const addressNested = Array.isArray(addressRoot?.data) ? addressRoot.data : [];",
                  "  const addressList = Array.isArray(addressRoot) ? addressRoot : (addressRows.length ? addressRows : (addressResults.length ? addressResults : addressNested));",
                  "  const selectedAddress = addressList[0] || addressRoot || address || {};",
                  "  const previousAddressId = pm.collectionVariables.get('addressId') || (pm.environment && pm.environment.get('addressId'));",
                  "  const nextAddressId = selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId;",
                  "  if (nextAddressId && previousAddressId && String(nextAddressId) !== String(previousAddressId)) {",
                  "    unsetVar('deliveryToken');",
                  "    unsetVar('deliveryTokenAddressId');",
                  "    unsetVar('deliveryCharge');",
                  "    unsetVar('deliveryDiscount');",
                  "  }",
                  "  setIf('addressId', selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId);",
                  "  setIf('countryId', selectedAddress?.country_id || selectedAddress?.countryId || data?.country_id || data?.countryId);",
                  "  setIf('stateId', selectedAddress?.state_id || selectedAddress?.stateId || data?.state_id || data?.stateId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /address/setdefault/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/address/setdefault/{{addressId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "address",
                "setdefault",
                "{{addressId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"fullname\": \"John Doe\",\n  \"phone\": \"+2348012345678\",\n  \"street\": \"12 Example St\",\n  \"city\": \"Lagos\",\n  \"state\": \"Lagos\",\n  \"country\": \"Nigeria\",\n  \"postalCode\": \"100001\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const addressRoot = data?.address || data;",
                  "  const addressRows = Array.isArray(addressRoot?.rows) ? addressRoot.rows : [];",
                  "  const addressResults = Array.isArray(addressRoot?.results) ? addressRoot.results : [];",
                  "  const addressNested = Array.isArray(addressRoot?.data) ? addressRoot.data : [];",
                  "  const addressList = Array.isArray(addressRoot) ? addressRoot : (addressRows.length ? addressRows : (addressResults.length ? addressResults : addressNested));",
                  "  const selectedAddress = addressList[0] || addressRoot || address || {};",
                  "  const previousAddressId = pm.collectionVariables.get('addressId') || (pm.environment && pm.environment.get('addressId'));",
                  "  const nextAddressId = selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId;",
                  "  if (nextAddressId && previousAddressId && String(nextAddressId) !== String(previousAddressId)) {",
                  "    unsetVar('deliveryToken');",
                  "    unsetVar('deliveryTokenAddressId');",
                  "    unsetVar('deliveryCharge');",
                  "    unsetVar('deliveryDiscount');",
                  "  }",
                  "  setIf('addressId', selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId);",
                  "  setIf('countryId', selectedAddress?.country_id || selectedAddress?.countryId || data?.country_id || data?.countryId);",
                  "  setIf('stateId', selectedAddress?.state_id || selectedAddress?.stateId || data?.state_id || data?.stateId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /address/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/address/{{addressId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "address",
                "{{addressId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const addressRoot = data?.address || data;",
                  "  const addressRows = Array.isArray(addressRoot?.rows) ? addressRoot.rows : [];",
                  "  const addressResults = Array.isArray(addressRoot?.results) ? addressRoot.results : [];",
                  "  const addressNested = Array.isArray(addressRoot?.data) ? addressRoot.data : [];",
                  "  const addressList = Array.isArray(addressRoot) ? addressRoot : (addressRows.length ? addressRows : (addressResults.length ? addressResults : addressNested));",
                  "  const selectedAddress = addressList[0] || addressRoot || address || {};",
                  "  const previousAddressId = pm.collectionVariables.get('addressId') || (pm.environment && pm.environment.get('addressId'));",
                  "  const nextAddressId = selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId;",
                  "  if (nextAddressId && previousAddressId && String(nextAddressId) !== String(previousAddressId)) {",
                  "    unsetVar('deliveryToken');",
                  "    unsetVar('deliveryTokenAddressId');",
                  "    unsetVar('deliveryCharge');",
                  "    unsetVar('deliveryDiscount');",
                  "  }",
                  "  setIf('addressId', selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId);",
                  "  setIf('countryId', selectedAddress?.country_id || selectedAddress?.countryId || data?.country_id || data?.countryId);",
                  "  setIf('stateId', selectedAddress?.state_id || selectedAddress?.stateId || data?.state_id || data?.stateId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /address/me",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/address/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "address",
                "me"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const addressRoot = data?.address || data;",
                  "  const addressRows = Array.isArray(addressRoot?.rows) ? addressRoot.rows : [];",
                  "  const addressResults = Array.isArray(addressRoot?.results) ? addressRoot.results : [];",
                  "  const addressNested = Array.isArray(addressRoot?.data) ? addressRoot.data : [];",
                  "  const addressList = Array.isArray(addressRoot) ? addressRoot : (addressRows.length ? addressRows : (addressResults.length ? addressResults : addressNested));",
                  "  const selectedAddress = addressList[0] || addressRoot || address || {};",
                  "  const previousAddressId = pm.collectionVariables.get('addressId') || (pm.environment && pm.environment.get('addressId'));",
                  "  const nextAddressId = selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId;",
                  "  if (nextAddressId && previousAddressId && String(nextAddressId) !== String(previousAddressId)) {",
                  "    unsetVar('deliveryToken');",
                  "    unsetVar('deliveryTokenAddressId');",
                  "    unsetVar('deliveryCharge');",
                  "    unsetVar('deliveryDiscount');",
                  "  }",
                  "  setIf('addressId', selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId);",
                  "  setIf('countryId', selectedAddress?.country_id || selectedAddress?.countryId || data?.country_id || data?.countryId);",
                  "  setIf('stateId', selectedAddress?.state_id || selectedAddress?.stateId || data?.state_id || data?.stateId);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "BANNER",
      "item": [
        {
          "name": "GET /banner/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/banner/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "banner",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /banner",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/banner",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "banner"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"title\": \"Spring Sale\",\n  \"imageUrl\": \"https://example.com/banner.jpg\",\n  \"position\": 1,\n  \"status\": \"active\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /banner/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/banner/{{bannerId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "banner",
                "{{bannerId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"title\": \"Spring Sale\",\n  \"imageUrl\": \"https://example.com/banner.jpg\",\n  \"position\": 1,\n  \"status\": \"active\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /banner/position/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/banner/position/{{bannerId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "banner",
                "position",
                "{{bannerId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"title\": \"Spring Sale\",\n  \"imageUrl\": \"https://example.com/banner.jpg\",\n  \"position\": 1,\n  \"status\": \"active\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /banner/status/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/banner/status/{{bannerId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "banner",
                "status",
                "{{bannerId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"title\": \"Spring Sale\",\n  \"imageUrl\": \"https://example.com/banner.jpg\",\n  \"position\": 1,\n  \"status\": \"active\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /banner/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/banner/{{bannerId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "banner",
                "{{bannerId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "BOOST REQUESTS",
      "item": [
        {
          "name": "GET /boost-requests",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/boost-requests",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "boost-requests"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /boost-requests/active",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/boost-requests/active",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "boost-requests",
                "active"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /boost-requests/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/boost-requests/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "boost-requests",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /boost-requests",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/boost-requests",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "boost-requests"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"storeId\": 123,\n  \"productId\": 456,\n  \"days\": 7,\n  \"amount\": 5000\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /boost-requests",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/boost-requests",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "boost-requests"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"storeId\": 123,\n  \"productId\": 456,\n  \"days\": 7,\n  \"amount\": 5000\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /boost-requests/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/boost-requests/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "boost-requests",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /boost-requests/approve",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/boost-requests/approve",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "boost-requests",
                "approve"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"storeId\": 123,\n  \"productId\": 456,\n  \"days\": 7,\n  \"amount\": 5000\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /boost-requests/priority",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/boost-requests/priority",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "boost-requests",
                "priority"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"storeId\": 123,\n  \"productId\": 456,\n  \"days\": 7,\n  \"amount\": 5000\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "BUSINESSTYPE",
      "item": [
        {
          "name": "GET /businesstype",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/businesstype",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "businesstype"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /businesstype",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/businesstype",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "businesstype"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"cart\": [\n    {\n      \"id\": 12,\n      \"userId\": {{userId}},\n      \"productId\": 13197,\n      \"variantId\": null,\n      \"storeId\": 4548,\n      \"quantity\": 1,\n      \"image\": \"https://example.com/product.jpg\",\n      \"totalPrice\": 5000,\n      \"productDetails\": {\n        \"image\": \"https://example.com/product.jpg\",\n        \"name\": \"Iphone 17 Max\",\n        \"price\": 5000\n      },\n      \"storeDetails\": {},\n      \"buyPrice\": 5000,\n      \"name\": \"Iphone 17 Max\"\n    }\n  ],\n  \"address\": {\n    \"id\": {{addressId}},\n    \"userId\": {{userId}},\n    \"flat\": \"\",\n    \"fullAddress\": \"Test address one\",\n    \"pin_code\": \"\",\n    \"state\": \"Lagos\",\n    \"city\": \"Ebute Metta\",\n    \"street\": \"Test address one\",\n    \"alt_phone\": \"08000000000\",\n    \"code\": \"+234\",\n    \"geo_location\": \"\",\n    \"type\": \"Home\",\n    \"lat\": null,\n    \"long\": null\n  },\n  \"total\": 5000\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /businesstype/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/businesstype/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "businesstype",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /businesstype/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/businesstype/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "businesstype",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "CALCULATE DELIVERY",
      "item": [
        {
          "name": "POST /calculate_delivery",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/calculate_delivery",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "calculate_delivery"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"cart\": [\n    {\n      \"id\": {{cartItemId}},\n      \"userId\": {{userId}},\n      \"productId\": {{productId}},\n      \"variantId\": {{variantId}},\n      \"storeId\": {{storeId}},\n      \"quantity\": {{orderQuantity}},\n      \"image\": \"{{productImage}}\",\n      \"totalPrice\": {{productPrice}},\n      \"buyPrice\": {{productPrice}},\n      \"name\": \"{{productName}}\",\n      \"productDetails\": {\n        \"image\": \"{{productImage}}\",\n        \"name\": \"{{productName}}\",\n        \"price\": {{productPrice}}\n      },\n      \"storeDetails\": {}\n    }\n  ],\n  \"address\": {\n    \"id\": {{addressId}},\n    \"userId\": {{userId}},\n    \"flat\": \"12\",\n    \"fullAddress\": \"12 Test Street, Ikeja, Lagos\",\n    \"pin_code\": \"100001\",\n    \"state\": \"Lagos\",\n    \"city\": \"Ikeja\",\n    \"street\": \"Test Street\",\n    \"alt_phone\": \"08000000000\",\n    \"code\": \"NG\",\n    \"geo_location\": \"\",\n    \"type\": \"home\",\n    \"lat\": null,\n    \"long\": null\n  },\n  \"total\": {{productPrice}}\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const deliveryToken = json?.token || data?.token;",
                  "  const deliveryTokenPayload = decodeJwtPayload(deliveryToken);",
                  "  setIf('deliveryToken', deliveryToken);",
                  "  setIf('deliveryTokenAddressId', deliveryTokenPayload?.data?.addressId || pm.collectionVariables.get('addressId'));",
                  "  setIf('deliveryCharge', data?.amount || data?.delivery_charge || data?.deliveryCharge);",
                  "  setIf('deliveryDiscount', data?.discount);",
                  "  setIf('productWeight', data?.totalWeight || data?.total_weight || pm.collectionVariables.get('productWeight'));",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /calculate_delivery/new",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/calculate_delivery/new",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "calculate_delivery",
                "new"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"cart\": [\n    {\n      \"weight\": {{productWeight}},\n      \"quantity\": {{orderQuantity}}\n    }\n  ],\n  \"address\": {\n    \"id\": {{addressId}},\n    \"country_id\": {{countryId}},\n    \"state_id\": {{stateId}}\n  }\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const deliveryToken = json?.token || data?.token;",
                  "  const deliveryTokenPayload = decodeJwtPayload(deliveryToken);",
                  "  setIf('deliveryToken', deliveryToken);",
                  "  setIf('deliveryTokenAddressId', deliveryTokenPayload?.data?.addressId || pm.collectionVariables.get('addressId'));",
                  "  setIf('deliveryCharge', data?.amount || data?.delivery_charge || data?.deliveryCharge);",
                  "  setIf('deliveryDiscount', data?.discount);",
                  "  setIf('productWeight', data?.totalWeight || data?.total_weight || pm.collectionVariables.get('productWeight'));",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /calculate_delivery/public",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/calculate_delivery/public",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "calculate_delivery",
                "public"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"cart\": [\n    {\n      \"id\": {{productId}},\n      \"productId\": {{productId}},\n      \"variantId\": {{variantId}},\n      \"storeId\": {{storeId}},\n      \"name\": \"{{productName}}\",\n      \"quantity\": {{orderQuantity}},\n      \"weight\": {{productWeight}},\n      \"totalPrice\": {{productPrice}}\n    }\n  ],\n  \"address\": {\n    \"id\": \"guest_{{userId}}\",\n    \"full_name\": \"Guest Buyer\",\n    \"phone_no\": \"08000000000\",\n    \"full_address\": \"12 Test Street, Ikeja, Lagos\",\n    \"country_id\": {{countryId}},\n    \"state_id\": {{stateId}},\n    \"country\": \"Nigeria\",\n    \"state\": \"Lagos\",\n    \"is_guest\": true\n  },\n  \"total\": {{productPrice}}\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const deliveryToken = json?.token || data?.token;",
                  "  const deliveryTokenPayload = decodeJwtPayload(deliveryToken);",
                  "  setIf('deliveryToken', deliveryToken);",
                  "  setIf('deliveryTokenAddressId', deliveryTokenPayload?.data?.addressId || pm.collectionVariables.get('addressId'));",
                  "  setIf('deliveryCharge', data?.amount || data?.delivery_charge || data?.deliveryCharge);",
                  "  setIf('deliveryDiscount', data?.discount);",
                  "  setIf('productWeight', data?.totalWeight || data?.total_weight || pm.collectionVariables.get('productWeight'));",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "CART",
      "item": [
        {
          "name": "GET /cart/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/cart/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "cart",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const cartRoot = data?.cart || data?.item || data;",
                  "  const cartRows = Array.isArray(cartRoot?.rows) ? cartRoot.rows : [];",
                  "  const cartResults = Array.isArray(cartRoot?.results) ? cartRoot.results : [];",
                  "  const cartNested = Array.isArray(cartRoot?.data) ? cartRoot.data : [];",
                  "  const cartList = Array.isArray(cartRoot) ? cartRoot : (cartRows.length ? cartRows : (cartResults.length ? cartResults : cartNested));",
                  "  const cartItem = cartList[0] || cartRoot || first || {};",
                  "  const cartProduct = cartItem?.product || cartItem?.productDetails || product || {};",
                  "  const cartStore = cartItem?.store || cartItem?.storeDetails || store || {};",
                  "  const cartVariant = cartItem?.variant || cartItem?.variantDetails || firstVariant || {};",
                  "  const cartQuantity = cartItem?.quantity || cartItem?.qty || pm.collectionVariables.get('orderQuantity') || 1;",
                  "  const cartPrice = cartItem?.price || cartItem?.buyPrice || cartItem?.unit_price || cartItem?.unitPrice || cartProduct?.price || cartProduct?.retail_rate || (cartItem?.totalPrice && cartQuantity ? Number(cartItem.totalPrice) / Number(cartQuantity) : undefined);",
                  "  const cartWeight = cartItem?.productWeight || cartItem?.product_weight || cartItem?.weight || cartProduct?.product_weight || cartProduct?.productWeight || cartProduct?.weight || pm.collectionVariables.get('productWeight') || 1;",
                  "  setIf('cartItemId', cartItem?.id || cartItem?._id || cartItem?.cartId || cartItem?.cart_id);",
                  "  setIf('productId', cartItem?.productId || cartItem?.product_id || cartProduct?._id || cartProduct?.id || cartProduct?.productId);",
                  "  setIf('productPid', cartItem?.pid || cartProduct?.pid);",
                  "  setIf('variantId', cartItem?.variantId ?? cartItem?.variant_id ?? cartVariant?.id ?? cartVariant?._id ?? 'null');",
                  "  setIf('storeId', cartItem?.storeId || cartItem?.store_id || cartProduct?.store_id || cartProduct?.storeId || cartStore?.id || cartStore?._id);",
                  "  setIf('orderQuantity', cartQuantity);",
                  "  setIf('productPrice', cartPrice);",
                  "  setIf('productName', cartItem?.name || cartProduct?.name || cartProduct?.title);",
                  "  setIf('productImage', cartItem?.image || cartProduct?.image || cartVariant?.image);",
                  "  setIf('productWeight', cartWeight);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /cart",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/cart",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "cart"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"productId\": {{productId}},\n  \"variantId\": {{variantId}},\n  \"quantity\": {{orderQuantity}}\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const cartRoot = data?.cart || data?.item || data;",
                  "  const cartRows = Array.isArray(cartRoot?.rows) ? cartRoot.rows : [];",
                  "  const cartResults = Array.isArray(cartRoot?.results) ? cartRoot.results : [];",
                  "  const cartNested = Array.isArray(cartRoot?.data) ? cartRoot.data : [];",
                  "  const cartList = Array.isArray(cartRoot) ? cartRoot : (cartRows.length ? cartRows : (cartResults.length ? cartResults : cartNested));",
                  "  const cartItem = cartList[0] || cartRoot || first || {};",
                  "  const cartProduct = cartItem?.product || cartItem?.productDetails || product || {};",
                  "  const cartStore = cartItem?.store || cartItem?.storeDetails || store || {};",
                  "  const cartVariant = cartItem?.variant || cartItem?.variantDetails || firstVariant || {};",
                  "  const cartQuantity = cartItem?.quantity || cartItem?.qty || pm.collectionVariables.get('orderQuantity') || 1;",
                  "  const cartPrice = cartItem?.price || cartItem?.buyPrice || cartItem?.unit_price || cartItem?.unitPrice || cartProduct?.price || cartProduct?.retail_rate || (cartItem?.totalPrice && cartQuantity ? Number(cartItem.totalPrice) / Number(cartQuantity) : undefined);",
                  "  const cartWeight = cartItem?.productWeight || cartItem?.product_weight || cartItem?.weight || cartProduct?.product_weight || cartProduct?.productWeight || cartProduct?.weight || pm.collectionVariables.get('productWeight') || 1;",
                  "  setIf('cartItemId', cartItem?.id || cartItem?._id || cartItem?.cartId || cartItem?.cart_id);",
                  "  setIf('productId', cartItem?.productId || cartItem?.product_id || cartProduct?._id || cartProduct?.id || cartProduct?.productId);",
                  "  setIf('productPid', cartItem?.pid || cartProduct?.pid);",
                  "  setIf('variantId', cartItem?.variantId ?? cartItem?.variant_id ?? cartVariant?.id ?? cartVariant?._id ?? 'null');",
                  "  setIf('storeId', cartItem?.storeId || cartItem?.store_id || cartProduct?.store_id || cartProduct?.storeId || cartStore?.id || cartStore?._id);",
                  "  setIf('orderQuantity', cartQuantity);",
                  "  setIf('productPrice', cartPrice);",
                  "  setIf('productName', cartItem?.name || cartProduct?.name || cartProduct?.title);",
                  "  setIf('productImage', cartItem?.image || cartProduct?.image || cartVariant?.image);",
                  "  setIf('productWeight', cartWeight);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /cart/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/cart/{{cartItemId}}?action=add",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "cart",
                "{{cartItemId}}?action=add"
              ],
              "query": [
                {
                  "key": "action",
                  "value": "add"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"userId\": 1,\n  \"items\": [\n    {\n      \"productId\": 1,\n      \"quantity\": 1\n    }\n  ]\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const cartRoot = data?.cart || data?.item || data;",
                  "  const cartRows = Array.isArray(cartRoot?.rows) ? cartRoot.rows : [];",
                  "  const cartResults = Array.isArray(cartRoot?.results) ? cartRoot.results : [];",
                  "  const cartNested = Array.isArray(cartRoot?.data) ? cartRoot.data : [];",
                  "  const cartList = Array.isArray(cartRoot) ? cartRoot : (cartRows.length ? cartRows : (cartResults.length ? cartResults : cartNested));",
                  "  const cartItem = cartList[0] || cartRoot || first || {};",
                  "  const cartProduct = cartItem?.product || cartItem?.productDetails || product || {};",
                  "  const cartStore = cartItem?.store || cartItem?.storeDetails || store || {};",
                  "  const cartVariant = cartItem?.variant || cartItem?.variantDetails || firstVariant || {};",
                  "  const cartQuantity = cartItem?.quantity || cartItem?.qty || pm.collectionVariables.get('orderQuantity') || 1;",
                  "  const cartPrice = cartItem?.price || cartItem?.buyPrice || cartItem?.unit_price || cartItem?.unitPrice || cartProduct?.price || cartProduct?.retail_rate || (cartItem?.totalPrice && cartQuantity ? Number(cartItem.totalPrice) / Number(cartQuantity) : undefined);",
                  "  const cartWeight = cartItem?.productWeight || cartItem?.product_weight || cartItem?.weight || cartProduct?.product_weight || cartProduct?.productWeight || cartProduct?.weight || pm.collectionVariables.get('productWeight') || 1;",
                  "  setIf('cartItemId', cartItem?.id || cartItem?._id || cartItem?.cartId || cartItem?.cart_id);",
                  "  setIf('productId', cartItem?.productId || cartItem?.product_id || cartProduct?._id || cartProduct?.id || cartProduct?.productId);",
                  "  setIf('productPid', cartItem?.pid || cartProduct?.pid);",
                  "  setIf('variantId', cartItem?.variantId ?? cartItem?.variant_id ?? cartVariant?.id ?? cartVariant?._id ?? 'null');",
                  "  setIf('storeId', cartItem?.storeId || cartItem?.store_id || cartProduct?.store_id || cartProduct?.storeId || cartStore?.id || cartStore?._id);",
                  "  setIf('orderQuantity', cartQuantity);",
                  "  setIf('productPrice', cartPrice);",
                  "  setIf('productName', cartItem?.name || cartProduct?.name || cartProduct?.title);",
                  "  setIf('productImage', cartItem?.image || cartProduct?.image || cartVariant?.image);",
                  "  setIf('productWeight', cartWeight);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /cart/clear-all",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/cart/clear-all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "cart",
                "clear-all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const cartRoot = data?.cart || data?.item || data;",
                  "  const cartRows = Array.isArray(cartRoot?.rows) ? cartRoot.rows : [];",
                  "  const cartResults = Array.isArray(cartRoot?.results) ? cartRoot.results : [];",
                  "  const cartNested = Array.isArray(cartRoot?.data) ? cartRoot.data : [];",
                  "  const cartList = Array.isArray(cartRoot) ? cartRoot : (cartRows.length ? cartRows : (cartResults.length ? cartResults : cartNested));",
                  "  const cartItem = cartList[0] || cartRoot || first || {};",
                  "  const cartProduct = cartItem?.product || cartItem?.productDetails || product || {};",
                  "  const cartStore = cartItem?.store || cartItem?.storeDetails || store || {};",
                  "  const cartVariant = cartItem?.variant || cartItem?.variantDetails || firstVariant || {};",
                  "  const cartQuantity = cartItem?.quantity || cartItem?.qty || pm.collectionVariables.get('orderQuantity') || 1;",
                  "  const cartPrice = cartItem?.price || cartItem?.buyPrice || cartItem?.unit_price || cartItem?.unitPrice || cartProduct?.price || cartProduct?.retail_rate || (cartItem?.totalPrice && cartQuantity ? Number(cartItem.totalPrice) / Number(cartQuantity) : undefined);",
                  "  const cartWeight = cartItem?.productWeight || cartItem?.product_weight || cartItem?.weight || cartProduct?.product_weight || cartProduct?.productWeight || cartProduct?.weight || pm.collectionVariables.get('productWeight') || 1;",
                  "  setIf('cartItemId', cartItem?.id || cartItem?._id || cartItem?.cartId || cartItem?.cart_id);",
                  "  setIf('productId', cartItem?.productId || cartItem?.product_id || cartProduct?._id || cartProduct?.id || cartProduct?.productId);",
                  "  setIf('productPid', cartItem?.pid || cartProduct?.pid);",
                  "  setIf('variantId', cartItem?.variantId ?? cartItem?.variant_id ?? cartVariant?.id ?? cartVariant?._id ?? 'null');",
                  "  setIf('storeId', cartItem?.storeId || cartItem?.store_id || cartProduct?.store_id || cartProduct?.storeId || cartStore?.id || cartStore?._id);",
                  "  setIf('orderQuantity', cartQuantity);",
                  "  setIf('productPrice', cartPrice);",
                  "  setIf('productName', cartItem?.name || cartProduct?.name || cartProduct?.title);",
                  "  setIf('productImage', cartItem?.image || cartProduct?.image || cartVariant?.image);",
                  "  setIf('productWeight', cartWeight);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /cart/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/cart/{{cartItemId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "cart",
                "{{cartItemId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const cartRoot = data?.cart || data?.item || data;",
                  "  const cartRows = Array.isArray(cartRoot?.rows) ? cartRoot.rows : [];",
                  "  const cartResults = Array.isArray(cartRoot?.results) ? cartRoot.results : [];",
                  "  const cartNested = Array.isArray(cartRoot?.data) ? cartRoot.data : [];",
                  "  const cartList = Array.isArray(cartRoot) ? cartRoot : (cartRows.length ? cartRows : (cartResults.length ? cartResults : cartNested));",
                  "  const cartItem = cartList[0] || cartRoot || first || {};",
                  "  const cartProduct = cartItem?.product || cartItem?.productDetails || product || {};",
                  "  const cartStore = cartItem?.store || cartItem?.storeDetails || store || {};",
                  "  const cartVariant = cartItem?.variant || cartItem?.variantDetails || firstVariant || {};",
                  "  const cartQuantity = cartItem?.quantity || cartItem?.qty || pm.collectionVariables.get('orderQuantity') || 1;",
                  "  const cartPrice = cartItem?.price || cartItem?.buyPrice || cartItem?.unit_price || cartItem?.unitPrice || cartProduct?.price || cartProduct?.retail_rate || (cartItem?.totalPrice && cartQuantity ? Number(cartItem.totalPrice) / Number(cartQuantity) : undefined);",
                  "  const cartWeight = cartItem?.productWeight || cartItem?.product_weight || cartItem?.weight || cartProduct?.product_weight || cartProduct?.productWeight || cartProduct?.weight || pm.collectionVariables.get('productWeight') || 1;",
                  "  setIf('cartItemId', cartItem?.id || cartItem?._id || cartItem?.cartId || cartItem?.cart_id);",
                  "  setIf('productId', cartItem?.productId || cartItem?.product_id || cartProduct?._id || cartProduct?.id || cartProduct?.productId);",
                  "  setIf('productPid', cartItem?.pid || cartProduct?.pid);",
                  "  setIf('variantId', cartItem?.variantId ?? cartItem?.variant_id ?? cartVariant?.id ?? cartVariant?._id ?? 'null');",
                  "  setIf('storeId', cartItem?.storeId || cartItem?.store_id || cartProduct?.store_id || cartProduct?.storeId || cartStore?.id || cartStore?._id);",
                  "  setIf('orderQuantity', cartQuantity);",
                  "  setIf('productPrice', cartPrice);",
                  "  setIf('productName', cartItem?.name || cartProduct?.name || cartProduct?.title);",
                  "  setIf('productImage', cartItem?.image || cartProduct?.image || cartVariant?.image);",
                  "  setIf('productWeight', cartWeight);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /cart/product/:productId",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/cart/product/{{productId}}?variantId={{variantId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "cart",
                "product",
                "{{productId}}?variantId={{variantId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const cartRoot = data?.cart || data?.item || data;",
                  "  const cartRows = Array.isArray(cartRoot?.rows) ? cartRoot.rows : [];",
                  "  const cartResults = Array.isArray(cartRoot?.results) ? cartRoot.results : [];",
                  "  const cartNested = Array.isArray(cartRoot?.data) ? cartRoot.data : [];",
                  "  const cartList = Array.isArray(cartRoot) ? cartRoot : (cartRows.length ? cartRows : (cartResults.length ? cartResults : cartNested));",
                  "  const cartItem = cartList[0] || cartRoot || first || {};",
                  "  const cartProduct = cartItem?.product || cartItem?.productDetails || product || {};",
                  "  const cartStore = cartItem?.store || cartItem?.storeDetails || store || {};",
                  "  const cartVariant = cartItem?.variant || cartItem?.variantDetails || firstVariant || {};",
                  "  const cartQuantity = cartItem?.quantity || cartItem?.qty || pm.collectionVariables.get('orderQuantity') || 1;",
                  "  const cartPrice = cartItem?.price || cartItem?.buyPrice || cartItem?.unit_price || cartItem?.unitPrice || cartProduct?.price || cartProduct?.retail_rate || (cartItem?.totalPrice && cartQuantity ? Number(cartItem.totalPrice) / Number(cartQuantity) : undefined);",
                  "  const cartWeight = cartItem?.productWeight || cartItem?.product_weight || cartItem?.weight || cartProduct?.product_weight || cartProduct?.productWeight || cartProduct?.weight || pm.collectionVariables.get('productWeight') || 1;",
                  "  setIf('cartItemId', cartItem?.id || cartItem?._id || cartItem?.cartId || cartItem?.cart_id);",
                  "  setIf('productId', cartItem?.productId || cartItem?.product_id || cartProduct?._id || cartProduct?.id || cartProduct?.productId);",
                  "  setIf('productPid', cartItem?.pid || cartProduct?.pid);",
                  "  setIf('variantId', cartItem?.variantId ?? cartItem?.variant_id ?? cartVariant?.id ?? cartVariant?._id ?? 'null');",
                  "  setIf('storeId', cartItem?.storeId || cartItem?.store_id || cartProduct?.store_id || cartProduct?.storeId || cartStore?.id || cartStore?._id);",
                  "  setIf('orderQuantity', cartQuantity);",
                  "  setIf('productPrice', cartPrice);",
                  "  setIf('productName', cartItem?.name || cartProduct?.name || cartProduct?.title);",
                  "  setIf('productImage', cartItem?.image || cartProduct?.image || cartVariant?.image);",
                  "  setIf('productWeight', cartWeight);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /cart/{{id}}",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/cart/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "cart",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const cartRoot = data?.cart || data?.item || data;",
                  "  const cartRows = Array.isArray(cartRoot?.rows) ? cartRoot.rows : [];",
                  "  const cartResults = Array.isArray(cartRoot?.results) ? cartRoot.results : [];",
                  "  const cartNested = Array.isArray(cartRoot?.data) ? cartRoot.data : [];",
                  "  const cartList = Array.isArray(cartRoot) ? cartRoot : (cartRows.length ? cartRows : (cartResults.length ? cartResults : cartNested));",
                  "  const cartItem = cartList[0] || cartRoot || first || {};",
                  "  const cartProduct = cartItem?.product || cartItem?.productDetails || product || {};",
                  "  const cartStore = cartItem?.store || cartItem?.storeDetails || store || {};",
                  "  const cartVariant = cartItem?.variant || cartItem?.variantDetails || firstVariant || {};",
                  "  const cartQuantity = cartItem?.quantity || cartItem?.qty || pm.collectionVariables.get('orderQuantity') || 1;",
                  "  const cartPrice = cartItem?.price || cartItem?.buyPrice || cartItem?.unit_price || cartItem?.unitPrice || cartProduct?.price || cartProduct?.retail_rate || (cartItem?.totalPrice && cartQuantity ? Number(cartItem.totalPrice) / Number(cartQuantity) : undefined);",
                  "  const cartWeight = cartItem?.productWeight || cartItem?.product_weight || cartItem?.weight || cartProduct?.product_weight || cartProduct?.productWeight || cartProduct?.weight || pm.collectionVariables.get('productWeight') || 1;",
                  "  setIf('cartItemId', cartItem?.id || cartItem?._id || cartItem?.cartId || cartItem?.cart_id);",
                  "  setIf('productId', cartItem?.productId || cartItem?.product_id || cartProduct?._id || cartProduct?.id || cartProduct?.productId);",
                  "  setIf('productPid', cartItem?.pid || cartProduct?.pid);",
                  "  setIf('variantId', cartItem?.variantId ?? cartItem?.variant_id ?? cartVariant?.id ?? cartVariant?._id ?? 'null');",
                  "  setIf('storeId', cartItem?.storeId || cartItem?.store_id || cartProduct?.store_id || cartProduct?.storeId || cartStore?.id || cartStore?._id);",
                  "  setIf('orderQuantity', cartQuantity);",
                  "  setIf('productPrice', cartPrice);",
                  "  setIf('productName', cartItem?.name || cartProduct?.name || cartProduct?.title);",
                  "  setIf('productImage', cartItem?.image || cartProduct?.image || cartVariant?.image);",
                  "  setIf('productWeight', cartWeight);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /cart/product/{{productId}}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/cart/product/{{productId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "cart",
                "product",
                "{{productId}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const cartRoot = data?.cart || data?.item || data;",
                  "  const cartRows = Array.isArray(cartRoot?.rows) ? cartRoot.rows : [];",
                  "  const cartResults = Array.isArray(cartRoot?.results) ? cartRoot.results : [];",
                  "  const cartNested = Array.isArray(cartRoot?.data) ? cartRoot.data : [];",
                  "  const cartList = Array.isArray(cartRoot) ? cartRoot : (cartRows.length ? cartRows : (cartResults.length ? cartResults : cartNested));",
                  "  const cartItem = cartList[0] || cartRoot || first || {};",
                  "  const cartProduct = cartItem?.product || cartItem?.productDetails || product || {};",
                  "  const cartStore = cartItem?.store || cartItem?.storeDetails || store || {};",
                  "  const cartVariant = cartItem?.variant || cartItem?.variantDetails || firstVariant || {};",
                  "  const cartQuantity = cartItem?.quantity || cartItem?.qty || pm.collectionVariables.get('orderQuantity') || 1;",
                  "  const cartPrice = cartItem?.price || cartItem?.buyPrice || cartItem?.unit_price || cartItem?.unitPrice || cartProduct?.price || cartProduct?.retail_rate || (cartItem?.totalPrice && cartQuantity ? Number(cartItem.totalPrice) / Number(cartQuantity) : undefined);",
                  "  const cartWeight = cartItem?.productWeight || cartItem?.product_weight || cartItem?.weight || cartProduct?.product_weight || cartProduct?.productWeight || cartProduct?.weight || pm.collectionVariables.get('productWeight') || 1;",
                  "  setIf('cartItemId', cartItem?.id || cartItem?._id || cartItem?.cartId || cartItem?.cart_id);",
                  "  setIf('productId', cartItem?.productId || cartItem?.product_id || cartProduct?._id || cartProduct?.id || cartProduct?.productId);",
                  "  setIf('productPid', cartItem?.pid || cartProduct?.pid);",
                  "  setIf('variantId', cartItem?.variantId ?? cartItem?.variant_id ?? cartVariant?.id ?? cartVariant?._id ?? 'null');",
                  "  setIf('storeId', cartItem?.storeId || cartItem?.store_id || cartProduct?.store_id || cartProduct?.storeId || cartStore?.id || cartStore?._id);",
                  "  setIf('orderQuantity', cartQuantity);",
                  "  setIf('productPrice', cartPrice);",
                  "  setIf('productName', cartItem?.name || cartProduct?.name || cartProduct?.title);",
                  "  setIf('productImage', cartItem?.image || cartProduct?.image || cartVariant?.image);",
                  "  setIf('productWeight', cartWeight);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /cart/{{id}}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/cart/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "cart",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const cartRoot = data?.cart || data?.item || data;",
                  "  const cartRows = Array.isArray(cartRoot?.rows) ? cartRoot.rows : [];",
                  "  const cartResults = Array.isArray(cartRoot?.results) ? cartRoot.results : [];",
                  "  const cartNested = Array.isArray(cartRoot?.data) ? cartRoot.data : [];",
                  "  const cartList = Array.isArray(cartRoot) ? cartRoot : (cartRows.length ? cartRows : (cartResults.length ? cartResults : cartNested));",
                  "  const cartItem = cartList[0] || cartRoot || first || {};",
                  "  const cartProduct = cartItem?.product || cartItem?.productDetails || product || {};",
                  "  const cartStore = cartItem?.store || cartItem?.storeDetails || store || {};",
                  "  const cartVariant = cartItem?.variant || cartItem?.variantDetails || firstVariant || {};",
                  "  const cartQuantity = cartItem?.quantity || cartItem?.qty || pm.collectionVariables.get('orderQuantity') || 1;",
                  "  const cartPrice = cartItem?.price || cartItem?.buyPrice || cartItem?.unit_price || cartItem?.unitPrice || cartProduct?.price || cartProduct?.retail_rate || (cartItem?.totalPrice && cartQuantity ? Number(cartItem.totalPrice) / Number(cartQuantity) : undefined);",
                  "  const cartWeight = cartItem?.productWeight || cartItem?.product_weight || cartItem?.weight || cartProduct?.product_weight || cartProduct?.productWeight || cartProduct?.weight || pm.collectionVariables.get('productWeight') || 1;",
                  "  setIf('cartItemId', cartItem?.id || cartItem?._id || cartItem?.cartId || cartItem?.cart_id);",
                  "  setIf('productId', cartItem?.productId || cartItem?.product_id || cartProduct?._id || cartProduct?.id || cartProduct?.productId);",
                  "  setIf('productPid', cartItem?.pid || cartProduct?.pid);",
                  "  setIf('variantId', cartItem?.variantId ?? cartItem?.variant_id ?? cartVariant?.id ?? cartVariant?._id ?? 'null');",
                  "  setIf('storeId', cartItem?.storeId || cartItem?.store_id || cartProduct?.store_id || cartProduct?.storeId || cartStore?.id || cartStore?._id);",
                  "  setIf('orderQuantity', cartQuantity);",
                  "  setIf('productPrice', cartPrice);",
                  "  setIf('productName', cartItem?.name || cartProduct?.name || cartProduct?.title);",
                  "  setIf('productImage', cartItem?.image || cartProduct?.image || cartVariant?.image);",
                  "  setIf('productWeight', cartWeight);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "CATEGORY",
      "item": [
        {
          "name": "GET /category",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/category",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "category"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('categoryId', category?.id || category?._id || data?.id || data?.categoryId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /category/featured",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/category/featured",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "category",
                "featured"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('categoryId', category?.id || category?._id || data?.id || data?.categoryId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /category/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/category/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "category",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('categoryId', category?.id || category?._id || data?.id || data?.categoryId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /category",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/category",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "category"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('categoryId', category?.id || category?._id || data?.id || data?.categoryId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /category/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/category/{{categoryId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "category",
                "{{categoryId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('categoryId', category?.id || category?._id || data?.id || data?.categoryId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /category/position/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/category/position/{{categoryId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "category",
                "position",
                "{{categoryId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('categoryId', category?.id || category?._id || data?.id || data?.categoryId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /category/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/category/{{categoryId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "category",
                "{{categoryId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('categoryId', category?.id || category?._id || data?.id || data?.categoryId);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "COUNTRIES",
      "item": [
        {
          "name": "GET /countries",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/countries",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "countries"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /countries/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/countries/{{countryId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "countries",
                "{{countryId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /countries",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/countries",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "countries"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /countries/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/countries/{{countryId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "countries",
                "{{countryId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /countries/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/countries/{{countryId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "countries",
                "{{countryId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "DASHBOARD",
      "item": [
        {
          "name": "GET /dashboard/count",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/dashboard/count",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "dashboard",
                "count"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /dashboard/statistics",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/dashboard/statistics",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "dashboard",
                "statistics"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /dashboard/order_statistics",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/dashboard/order_statistics",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "dashboard",
                "order_statistics"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "DELIVERYCHARGE",
      "item": [
        {
          "name": "GET /deliverycharge",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/deliverycharge",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "deliverycharge"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /deliverycharge",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/deliverycharge",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "deliverycharge"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /deliverycharge/upsert",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/deliverycharge/upsert",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "deliverycharge",
                "upsert"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /deliverycharge",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/deliverycharge",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "deliverycharge"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /deliverycharge/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/deliverycharge/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "deliverycharge",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "DELIVERY",
      "item": [
        {
          "name": "POST /delivery/company/register",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/company/register",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "company",
                "register"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /delivery/driver/register",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/driver/register",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "driver",
                "register"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/companies",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/companies",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "companies"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/companies/approved",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/companies/approved",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "companies",
                "approved"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/company/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/company/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "company",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /delivery/company/:id/status",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/company/{{id}}/status",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "company",
                "{{id}}",
                "status"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/drivers",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/drivers",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "drivers"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/driver/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/driver/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "driver",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /delivery/driver/:id/status",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/driver/{{id}}/status",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "driver",
                "{{id}}",
                "status"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /delivery/invitation/send",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/invitation/send",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "invitation",
                "send"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/invitation/driver/:driverId",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/invitation/driver/{{driverId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "invitation",
                "driver",
                "{{driverId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/invitation/company/:companyId",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/invitation/company/{{companyId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "invitation",
                "company",
                "{{companyId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /delivery/invitation/:id/accept",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/invitation/{{id}}/accept",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "invitation",
                "{{id}}",
                "accept"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /delivery/invitation/:id/reject",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/invitation/{{id}}/reject",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "invitation",
                "{{id}}",
                "reject"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/orders/available",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/orders/available",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "orders",
                "available"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/orders/debug",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/orders/debug",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "orders",
                "debug"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /delivery/order/:orderId/accept",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/order/{{orderId}}/accept",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "order",
                "{{orderId}}",
                "accept"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/company/:companyId/drivers",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/company/{{companyId}}/drivers",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "company",
                "{{companyId}}",
                "drivers"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/company/:companyId/drivers/available",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/company/{{companyId}}/drivers/available",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "company",
                "{{companyId}}",
                "drivers",
                "available"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/company/:companyId/orders",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/company/{{companyId}}/orders",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "company",
                "{{companyId}}",
                "orders"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/company/:companyId/orders/accepted",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/company/{{companyId}}/orders/accepted",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "company",
                "{{companyId}}",
                "orders",
                "accepted"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/company/:companyId/orders/accepted-with-drivers",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/company/{{companyId}}/orders/accepted-with-drivers",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "company",
                "{{companyId}}",
                "orders",
                "accepted-with-drivers"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /delivery/order/:orderId/pickup",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/order/{{orderId}}/pickup",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "order",
                "{{orderId}}",
                "pickup"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/driver/:driverId/orders",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/driver/{{driverId}}/orders",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "driver",
                "{{driverId}}",
                "orders"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/driver/:driverId/orders/pending",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/driver/{{driverId}}/orders/pending",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "driver",
                "{{driverId}}",
                "orders",
                "pending"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/driver/:driverId/orders/completed",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/driver/{{driverId}}/orders/completed",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "driver",
                "{{driverId}}",
                "orders",
                "completed"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/driver/:driverId/orders/available",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/driver/{{driverId}}/orders/available",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "driver",
                "{{driverId}}",
                "orders",
                "available"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /delivery/driver/:driverId/order/:orderId/select",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/driver/{{driverId}}/order/{{orderId}}/select",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "driver",
                "{{driverId}}",
                "order",
                "{{orderId}}",
                "select"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /delivery/order/:orderId/deliver",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/order/{{orderId}}/deliver",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "order",
                "{{orderId}}",
                "deliver"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /delivery/orders/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/delivery/orders/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "delivery",
                "orders",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "DISTANCECHARGE",
      "item": [
        {
          "name": "GET /distancecharge",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/distancecharge",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "distancecharge"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /distancecharge",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/distancecharge",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "distancecharge"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /distancecharge/upsert",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/distancecharge/upsert",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "distancecharge",
                "upsert"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /distancecharge",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/distancecharge",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "distancecharge"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /distancecharge/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/distancecharge/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "distancecharge",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "ENQUIRY",
      "item": [
        {
          "name": "GET /Enquiry/get",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/Enquiry/get",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "Enquiry",
                "get"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('enquiryId', enquiry?.id || enquiry?._id || data?.id);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /Enquiry/post",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/Enquiry/post",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "Enquiry",
                "post"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('enquiryId', enquiry?.id || enquiry?._id || data?.id);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /Enquiry/update/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/Enquiry/update/{{enquiryId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "Enquiry",
                "update",
                "{{enquiryId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('enquiryId', enquiry?.id || enquiry?._id || data?.id);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PATCH /Enquiry/patch/:id",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/Enquiry/patch/{{enquiryId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "Enquiry",
                "patch",
                "{{enquiryId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('enquiryId', enquiry?.id || enquiry?._id || data?.id);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /Enquiry/delete/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/Enquiry/delete/{{enquiryId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "Enquiry",
                "delete",
                "{{enquiryId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('enquiryId', enquiry?.id || enquiry?._id || data?.id);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "FEATURED PRODUCTS",
      "item": [
        {
          "name": "GET /featured-products/products",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/featured-products/products",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "featured-products",
                "products"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /featured-products/position/:position",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/featured-products/position/{{position}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "featured-products",
                "position",
                "{{position}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /featured-products/position/:position/products",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/featured-products/position/{{position}}/products",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "featured-products",
                "position",
                "{{position}}",
                "products"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "GOOGLE PROXY",
      "item": [
        {
          "name": "GET /google-proxy/placepicker",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/google-proxy/placepicker",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "google-proxy",
                "placepicker"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /google-proxy/autocomplete",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/google-proxy/autocomplete",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "google-proxy",
                "autocomplete"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "IMG COMPRESS",
      "item": [
        {
          "name": "POST /img_compress/compress",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/img_compress/compress",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "img_compress",
                "compress"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /img_compress/file",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/img_compress/file",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "img_compress",
                "file"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /img_compress/file/both",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/img_compress/file/both",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "img_compress",
                "file",
                "both"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /img_compress/video",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/img_compress/video",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "img_compress",
                "video"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "INDIVIDUAL SELLER",
      "item": [
        {
          "name": "GET /individual_seller/pgn",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/individual_seller/pgn",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "individual_seller",
                "pgn"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /individual_seller/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/individual_seller/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "individual_seller",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /individual_seller/create",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/individual_seller/create",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "individual_seller",
                "create"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /individual_seller/send_mail",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/individual_seller/send_mail",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "individual_seller",
                "send_mail"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /individual_seller/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/individual_seller/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "individual_seller",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /individual_seller/status/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/individual_seller/status/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "individual_seller",
                "status",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /individual_seller/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/individual_seller/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "individual_seller",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "INVOICE",
      "item": [
        {
          "name": "POST /invoice/create",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/invoice/create",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "invoice",
                "create"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /invoice/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/invoice/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "invoice",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /invoice/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/invoice/{{invoiceId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "invoice",
                "{{invoiceId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /invoice/get/:token",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/invoice/get/{{invoiceToken}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "invoice",
                "get",
                "{{invoiceToken}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /invoice/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/invoice/{{invoiceId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "invoice",
                "{{invoiceId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /invoice/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/invoice/{{invoiceId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "invoice",
                "{{invoiceId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "LANDING",
      "item": [
        {
          "name": "GET /landing",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/landing",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "landing"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "NEW ADDRESS",
      "item": [
        {
          "name": "GET /new-address",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/new-address",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "new-address"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const addressRoot = data?.address || data;",
                  "  const addressRows = Array.isArray(addressRoot?.rows) ? addressRoot.rows : [];",
                  "  const addressResults = Array.isArray(addressRoot?.results) ? addressRoot.results : [];",
                  "  const addressNested = Array.isArray(addressRoot?.data) ? addressRoot.data : [];",
                  "  const addressList = Array.isArray(addressRoot) ? addressRoot : (addressRows.length ? addressRows : (addressResults.length ? addressResults : addressNested));",
                  "  const selectedAddress = addressList[0] || addressRoot || address || {};",
                  "  const previousAddressId = pm.collectionVariables.get('addressId') || (pm.environment && pm.environment.get('addressId'));",
                  "  const nextAddressId = selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId;",
                  "  if (nextAddressId && previousAddressId && String(nextAddressId) !== String(previousAddressId)) {",
                  "    unsetVar('deliveryToken');",
                  "    unsetVar('deliveryTokenAddressId');",
                  "    unsetVar('deliveryCharge');",
                  "    unsetVar('deliveryDiscount');",
                  "  }",
                  "  setIf('addressId', selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId);",
                  "  setIf('countryId', selectedAddress?.country_id || selectedAddress?.countryId || data?.country_id || data?.countryId);",
                  "  setIf('stateId', selectedAddress?.state_id || selectedAddress?.stateId || data?.state_id || data?.stateId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /new-address/me",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/new-address/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "new-address",
                "me"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const addressRoot = data?.address || data;",
                  "  const addressRows = Array.isArray(addressRoot?.rows) ? addressRoot.rows : [];",
                  "  const addressResults = Array.isArray(addressRoot?.results) ? addressRoot.results : [];",
                  "  const addressNested = Array.isArray(addressRoot?.data) ? addressRoot.data : [];",
                  "  const addressList = Array.isArray(addressRoot) ? addressRoot : (addressRows.length ? addressRows : (addressResults.length ? addressResults : addressNested));",
                  "  const selectedAddress = addressList[0] || addressRoot || address || {};",
                  "  const previousAddressId = pm.collectionVariables.get('addressId') || (pm.environment && pm.environment.get('addressId'));",
                  "  const nextAddressId = selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId;",
                  "  if (nextAddressId && previousAddressId && String(nextAddressId) !== String(previousAddressId)) {",
                  "    unsetVar('deliveryToken');",
                  "    unsetVar('deliveryTokenAddressId');",
                  "    unsetVar('deliveryCharge');",
                  "    unsetVar('deliveryDiscount');",
                  "  }",
                  "  setIf('addressId', selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId);",
                  "  setIf('countryId', selectedAddress?.country_id || selectedAddress?.countryId || data?.country_id || data?.countryId);",
                  "  setIf('stateId', selectedAddress?.state_id || selectedAddress?.stateId || data?.state_id || data?.stateId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /new-address/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/new-address/{{addressId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "new-address",
                "{{addressId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const addressRoot = data?.address || data;",
                  "  const addressRows = Array.isArray(addressRoot?.rows) ? addressRoot.rows : [];",
                  "  const addressResults = Array.isArray(addressRoot?.results) ? addressRoot.results : [];",
                  "  const addressNested = Array.isArray(addressRoot?.data) ? addressRoot.data : [];",
                  "  const addressList = Array.isArray(addressRoot) ? addressRoot : (addressRows.length ? addressRows : (addressResults.length ? addressResults : addressNested));",
                  "  const selectedAddress = addressList[0] || addressRoot || address || {};",
                  "  const previousAddressId = pm.collectionVariables.get('addressId') || (pm.environment && pm.environment.get('addressId'));",
                  "  const nextAddressId = selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId;",
                  "  if (nextAddressId && previousAddressId && String(nextAddressId) !== String(previousAddressId)) {",
                  "    unsetVar('deliveryToken');",
                  "    unsetVar('deliveryTokenAddressId');",
                  "    unsetVar('deliveryCharge');",
                  "    unsetVar('deliveryDiscount');",
                  "  }",
                  "  setIf('addressId', selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId);",
                  "  setIf('countryId', selectedAddress?.country_id || selectedAddress?.countryId || data?.country_id || data?.countryId);",
                  "  setIf('stateId', selectedAddress?.state_id || selectedAddress?.stateId || data?.state_id || data?.stateId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /new-address",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/new-address",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "new-address"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"address_type\": \"home\",\n  \"full_address\": \"12 Test Street, Ikeja, Lagos\",\n  \"pincode\": \"100001\",\n  \"phone_no\": \"08000000000\",\n  \"country_id\": {{countryId}},\n  \"state_id\": {{stateId}}\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const addressRoot = data?.address || data;",
                  "  const addressRows = Array.isArray(addressRoot?.rows) ? addressRoot.rows : [];",
                  "  const addressResults = Array.isArray(addressRoot?.results) ? addressRoot.results : [];",
                  "  const addressNested = Array.isArray(addressRoot?.data) ? addressRoot.data : [];",
                  "  const addressList = Array.isArray(addressRoot) ? addressRoot : (addressRows.length ? addressRows : (addressResults.length ? addressResults : addressNested));",
                  "  const selectedAddress = addressList[0] || addressRoot || address || {};",
                  "  const previousAddressId = pm.collectionVariables.get('addressId') || (pm.environment && pm.environment.get('addressId'));",
                  "  const nextAddressId = selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId;",
                  "  if (nextAddressId && previousAddressId && String(nextAddressId) !== String(previousAddressId)) {",
                  "    unsetVar('deliveryToken');",
                  "    unsetVar('deliveryTokenAddressId');",
                  "    unsetVar('deliveryCharge');",
                  "    unsetVar('deliveryDiscount');",
                  "  }",
                  "  setIf('addressId', selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId);",
                  "  setIf('countryId', selectedAddress?.country_id || selectedAddress?.countryId || data?.country_id || data?.countryId);",
                  "  setIf('stateId', selectedAddress?.state_id || selectedAddress?.stateId || data?.state_id || data?.stateId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /new-address/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/new-address/{{addressId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "new-address",
                "{{addressId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"fullname\": \"John Doe\",\n  \"phone\": \"+2348012345678\",\n  \"street\": \"12 Example St\",\n  \"city\": \"Lagos\",\n  \"state\": \"Lagos\",\n  \"country\": \"Nigeria\",\n  \"postalCode\": \"100001\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const addressRoot = data?.address || data;",
                  "  const addressRows = Array.isArray(addressRoot?.rows) ? addressRoot.rows : [];",
                  "  const addressResults = Array.isArray(addressRoot?.results) ? addressRoot.results : [];",
                  "  const addressNested = Array.isArray(addressRoot?.data) ? addressRoot.data : [];",
                  "  const addressList = Array.isArray(addressRoot) ? addressRoot : (addressRows.length ? addressRows : (addressResults.length ? addressResults : addressNested));",
                  "  const selectedAddress = addressList[0] || addressRoot || address || {};",
                  "  const previousAddressId = pm.collectionVariables.get('addressId') || (pm.environment && pm.environment.get('addressId'));",
                  "  const nextAddressId = selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId;",
                  "  if (nextAddressId && previousAddressId && String(nextAddressId) !== String(previousAddressId)) {",
                  "    unsetVar('deliveryToken');",
                  "    unsetVar('deliveryTokenAddressId');",
                  "    unsetVar('deliveryCharge');",
                  "    unsetVar('deliveryDiscount');",
                  "  }",
                  "  setIf('addressId', selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId);",
                  "  setIf('countryId', selectedAddress?.country_id || selectedAddress?.countryId || data?.country_id || data?.countryId);",
                  "  setIf('stateId', selectedAddress?.state_id || selectedAddress?.stateId || data?.state_id || data?.stateId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /new-address/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/new-address/{{addressId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "new-address",
                "{{addressId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const addressRoot = data?.address || data;",
                  "  const addressRows = Array.isArray(addressRoot?.rows) ? addressRoot.rows : [];",
                  "  const addressResults = Array.isArray(addressRoot?.results) ? addressRoot.results : [];",
                  "  const addressNested = Array.isArray(addressRoot?.data) ? addressRoot.data : [];",
                  "  const addressList = Array.isArray(addressRoot) ? addressRoot : (addressRows.length ? addressRows : (addressResults.length ? addressResults : addressNested));",
                  "  const selectedAddress = addressList[0] || addressRoot || address || {};",
                  "  const previousAddressId = pm.collectionVariables.get('addressId') || (pm.environment && pm.environment.get('addressId'));",
                  "  const nextAddressId = selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId;",
                  "  if (nextAddressId && previousAddressId && String(nextAddressId) !== String(previousAddressId)) {",
                  "    unsetVar('deliveryToken');",
                  "    unsetVar('deliveryTokenAddressId');",
                  "    unsetVar('deliveryCharge');",
                  "    unsetVar('deliveryDiscount');",
                  "  }",
                  "  setIf('addressId', selectedAddress?.id || selectedAddress?._id || selectedAddress?.addressId || data?.id || data?.addressId);",
                  "  setIf('countryId', selectedAddress?.country_id || selectedAddress?.countryId || data?.country_id || data?.countryId);",
                  "  setIf('stateId', selectedAddress?.state_id || selectedAddress?.stateId || data?.state_id || data?.stateId);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "NEW DISTANCE CHARGE",
      "item": [
        {
          "name": "GET /new-distance-charge",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/new-distance-charge",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "new-distance-charge"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /new-distance-charge/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/new-distance-charge/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "new-distance-charge",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /new-distance-charge",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/new-distance-charge",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "new-distance-charge"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /new-distance-charge/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/new-distance-charge/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "new-distance-charge",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /new-distance-charge/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/new-distance-charge/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "new-distance-charge",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "NOTIFICATIONS",
      "item": [
        {
          "name": "GET /notifications/test/:token",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/notifications/test/{{token}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "notifications",
                "test",
                "{{token}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /notifications/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/notifications/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "notifications",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PATCH /notifications/read/:id",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/notifications/read/{{notificationId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "notifications",
                "read",
                "{{notificationId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "OFFERS",
      "item": [
        {
          "name": "GET /offers",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/offers",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "offers"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /offers/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/offers/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "offers",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /offers/auth",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/offers/auth",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "offers",
                "auth"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /offers/:slug",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/offers/{{slug}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "offers",
                "{{slug}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /offers",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/offers",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "offers"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /offers/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/offers/{{offerId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "offers",
                "{{offerId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /offers/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/offers/{{offerId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "offers",
                "{{offerId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /offers/offer_product/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/offers/offer_product/{{offerId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "offers",
                "offer_product",
                "{{offerId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "ORDER",
      "item": [
        {
          "name": "GET /order/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/store",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/store",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "store"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/getall",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/getall",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "getall"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/buy_again",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/buy_again",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "buy_again"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/store/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/store/{{orderId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "store",
                "{{orderId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/user/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/user/{{orderId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "user",
                "{{orderId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/details/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/details/{{orderId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "details",
                "{{orderId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/get_one/admin/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/get_one/admin/{{orderId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "get_one",
                "admin",
                "{{orderId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/get_one/seller/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/get_one/seller/{{orderId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "get_one",
                "seller",
                "{{orderId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/get_one/user/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/get_one/user/{{orderId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "get_one",
                "user",
                "{{orderId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /order",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"cart\": [\n    {\n      \"id\": {{cartItemId}},\n      \"productId\": {{productId}},\n      \"variantId\": {{variantId}},\n      \"storeId\": {{storeId}},\n      \"quantity\": {{orderQuantity}}\n    }\n  ],\n  \"payment\": {\n    \"type\": \"paystack\",\n    \"callback_url\": \"{{callbackUrl}}\"\n  },\n  \"address\": {\n    \"id\": {{addressId}}\n  },\n  \"charges\": {\n    \"token\": \"{{deliveryToken}}\"\n  }\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            },
            {
              "listen": "prerequest",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "const addressId = pm.variables.get('addressId');",
                  "const deliveryToken = pm.variables.get('deliveryToken');",
                  "let tokenAddressId = pm.variables.get('deliveryTokenAddressId');",
                  "",
                  "const decodeJwtPayload = (token) => {",
                  "  try {",
                  "    if (!token || String(token).split('.').length < 2) return null;",
                  "    let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "    while (payload.length % 4) payload += '=';",
                  "    return JSON.parse(atob(payload));",
                  "  } catch (error) {",
                  "    console.log('Unable to decode delivery token', error);",
                  "    return null;",
                  "  }",
                  "};",
                  "",
                  "if (!deliveryToken) {",
                  "  throw new Error('Missing deliveryToken. Run POST /calculate_delivery/new after selecting address and cart, then retry /order.');",
                  "}",
                  "",
                  "if (!tokenAddressId) {",
                  "  const decoded = decodeJwtPayload(deliveryToken);",
                  "  tokenAddressId = decoded?.data?.addressId;",
                  "  if (tokenAddressId !== undefined && tokenAddressId !== null) {",
                  "    pm.collectionVariables.set('deliveryTokenAddressId', String(tokenAddressId));",
                  "    if (pm.environment) pm.environment.set('deliveryTokenAddressId', String(tokenAddressId));",
                  "  }",
                  "}",
                  "",
                  "if (addressId && tokenAddressId && String(addressId) !== String(tokenAddressId)) {",
                  "  throw new Error(`Stale deliveryToken: token addressId=${tokenAddressId}, order addressId=${addressId}. Run POST /calculate_delivery/new again, then retry /order.`);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /order/update_status/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/update_status/{{orderId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "update_status",
                "{{orderId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"status\": \"processing\",\n  \"remark\": \"Order is being processed\",\n  \"delivery_date\": \"2026-04-10T00:00:00.000Z\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /order/update_payment/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/update_payment/{{orderId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "update_payment",
                "{{orderId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"cart\": [\n    {\n      \"id\": \"{{cartItemId}}\",\n      \"productId\": \"{{productId}}\",\n      \"variantId\": \"{{variantId}}\",\n      \"storeId\": \"{{storeId}}\",\n      \"quantity\": \"{{orderQuantity}}\"\n    }\n  ],\n  \"payment\": {\n    \"type\": \"paystack\",\n    \"callback_url\": \"{{callbackUrl}}\"\n  },\n  \"address\": {\n    \"id\": \"{{addressId}}\"\n  },\n  \"charges\": {\n    \"token\": \"{{deliveryToken}}\"\n  }\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /order/cancel_order/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/cancel_order/{{orderId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "cancel_order",
                "{{orderId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"cart\": [\n    {\n      \"id\": \"{{cartItemId}}\",\n      \"productId\": \"{{productId}}\",\n      \"variantId\": \"{{variantId}}\",\n      \"storeId\": \"{{storeId}}\",\n      \"quantity\": \"{{orderQuantity}}\"\n    }\n  ],\n  \"payment\": {\n    \"type\": \"paystack\",\n    \"callback_url\": \"{{callbackUrl}}\"\n  },\n  \"address\": {\n    \"id\": \"{{addressId}}\"\n  },\n  \"charges\": {\n    \"token\": \"{{deliveryToken}}\"\n  }\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /order/guest/update_status/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/guest/update_status/{{orderId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "guest",
                "update_status",
                "{{orderId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"status\": \"processing\",\n  \"remark\": \"Order is being processed\",\n  \"delivery_date\": \"2026-04-10T00:00:00.000Z\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /order/guest",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/guest",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "guest"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"guest_info\": {\n    \"email\": \"{{guestEmail}}\",\n    \"first_name\": \"Arua\",\n    \"last_name\": \"Ibiam\",\n    \"phone\": \"07066026820\"\n  },\n  \"delivery_address\": {\n    \"id\": \"guest_address_001\",\n    \"full_name\": \"Arua Ibiam\",\n    \"phone_no\": \"07066026820\",\n    \"full_address\": \"4 Test street, Lagos\",\n    \"city\": \"Lagos\",\n    \"state\": \"Lagos\",\n    \"state_id\": 1,\n    \"country\": \"Nigeria\",\n    \"country_id\": 8\n  },\n  \"cart_items\": [\n    {\n      \"product_id\": 13197,\n      \"store_id\": 4548,\n      \"product_name\": \"Iphone 17 Max\",\n      \"quantity\": 1,\n      \"unit_price\": 5000,\n      \"total_price\": 5000\n    }\n  ],\n  \"payment\": {\n    \"payment_reference\": \"{{paymentReference}}\",\n    \"transaction_reference\": \"{{paymentReference}}\",\n    \"payment_status\": \"success\"\n  },\n  \"delivery\": {\n    \"delivery_token\": \"{{deliveryToken}}\"\n  },\n  \"order_summary\": {\n    \"subtotal\": 5000,\n    \"delivery_fee\": 0,\n    \"discount\": 0,\n    \"tax\": 0,\n    \"total\": 5000\n  }\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /order/guest/orders",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/guest/orders",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "guest",
                "orders"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"{{guestEmail}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/guest/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/guest/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "guest",
                "all"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /order/guest/reconcile/:id",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/guest/reconcile/{{guestCheckoutId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "guest",
                "reconcile",
                "{{guestCheckoutId}}"
              ]
            },
            "description": "Admin-only replay for one paid orphaned guest checkout. Use an id from GET /order/guest/all where record_type is orphaned_guest_checkout and status is payment_received_processing.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"cart\": [\n    {\n      \"id\": \"{{cartItemId}}\",\n      \"productId\": \"{{productId}}\",\n      \"variantId\": \"{{variantId}}\",\n      \"storeId\": \"{{storeId}}\",\n      \"quantity\": \"{{orderQuantity}}\"\n    }\n  ],\n  \"payment\": {\n    \"type\": \"paystack\",\n    \"callback_url\": \"{{callbackUrl}}\"\n  },\n  \"address\": {\n    \"id\": \"{{addressId}}\"\n  },\n  \"charges\": {\n    \"token\": \"{{deliveryToken}}\"\n  }\n}"
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /order/guest/reconcile-bulk",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/guest/reconcile-bulk",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "guest",
                "reconcile-bulk"
              ]
            },
            "description": "Admin-only bulk replay for paid orphaned guest checkouts. Returns a summary with reconciled, skipped, and failed counts.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"cart\": [\n    {\n      \"id\": \"{{cartItemId}}\",\n      \"productId\": \"{{productId}}\",\n      \"variantId\": \"{{variantId}}\",\n      \"storeId\": \"{{storeId}}\",\n      \"quantity\": \"{{orderQuantity}}\"\n    }\n  ],\n  \"payment\": {\n    \"type\": \"paystack\",\n    \"callback_url\": \"{{callbackUrl}}\"\n  },\n  \"address\": {\n    \"id\": \"{{addressId}}\"\n  },\n  \"charges\": {\n    \"token\": \"{{deliveryToken}}\"\n  }\n}"
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /mail/test",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/mail/test",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "mail",
                "test"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"to\": \"{{userEmail}}\",\n  \"subject\": \"Mailtrap connectivity test\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "description": "Admin-only mail smoke test. Sends one simple email using the currently active provider, such as Mailtrap."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/guest/store",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/guest/store",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "guest",
                "store"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/store/stats",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/store/stats",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "store",
                "stats"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/guest/details/{{orderId}}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/guest/details/{{orderId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "guest",
                "details",
                "{{orderId}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /order/guest/reconcile/{{orderId}}",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/guest/reconcile/{{orderId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "guest",
                "reconcile",
                "{{orderId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"cart\": [\n    {\n      \"id\": \"{{cartItemId}}\",\n      \"productId\": \"{{productId}}\",\n      \"variantId\": \"{{variantId}}\",\n      \"storeId\": \"{{storeId}}\",\n      \"quantity\": \"{{orderQuantity}}\"\n    }\n  ],\n  \"payment\": {\n    \"type\": \"paystack\",\n    \"callback_url\": \"{{callbackUrl}}\"\n  },\n  \"address\": {\n    \"id\": \"{{addressId}}\"\n  },\n  \"charges\": {\n    \"token\": \"{{deliveryToken}}\"\n  }\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/me",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "me"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ],
          "response": []
        },
        {
          "name": "GET /order/by-user/:userId",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/by-user/{{userId}}?page=1&take=10&status=&sort=&order=DESC",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "by-user",
                "{{userId}}?page=1&take=10&status=&sort=&order=DESC"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/by-user/{{userId}}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/by-user/{{userId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "by-user",
                "{{userId}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /order/track/{{reference}}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/order/track/{{reference}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "order",
                "track",
                "{{reference}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const orderCandidate = order || first;",
                  "  setIf('orderId', orderCandidate?.id || data?.id);",
                  "  setIf('orderPublicId', orderCandidate?.order_id || data?.order_id);",
                  "  setIf('paymentReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('reconcileReference', data?.reference || payment?.ref || orderCandidate?.payment_reference || orderCandidate?.transaction_reference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || data?.authorizationUrl || payment?.authorization_url);",
                  "  setIf('paystackAccessCode', data?.access_code || data?.accessCode || payment?.access_code);",
                  "  setIf('guestEmail', orderCandidate?.guest_email || data?.email);",
                  "  setIf('storeId', orderCandidate?.storeId || store?.id || data?.storeId);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "ORDERSTATUS",
      "item": [
        {
          "name": "GET /orderStatus/all/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/orderStatus/all/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "orderStatus",
                "all",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "SUBSTITUTION",
      "item": [
        {
          "name": "GET /substitution/details/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/substitution/details/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "substitution",
                "details",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /substitution/five-mint-check",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/substitution/five-mint-check",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "substitution",
                "five-mint-check"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /substitution/create",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/substitution/create",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "substitution",
                "create"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /substitution/substitute/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/substitution/substitute/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "substitution",
                "substitute",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /substitution/update_order/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/substitution/update_order/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "substitution",
                "update_order",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PAYMENT GATEWAY",
      "item": [
        {
          "name": "GET /payment_gateway/token",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/payment_gateway/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "payment_gateway",
                "token"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /payment_gateway/order",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/payment_gateway/order",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "payment_gateway",
                "order"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /payment_gateway/details",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/payment_gateway/details",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "payment_gateway",
                "details"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /payment_gateway/refund",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/payment_gateway/refund",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "payment_gateway",
                "refund"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PAYMENTLOG",
      "item": [
        {
          "name": "POST /paymentlog",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paymentlog",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paymentlog"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PAYMENT SPLITS",
      "item": [
        {
          "name": "POST /payment-splits/create/:orderId",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/payment-splits/create/{{orderId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "payment-splits",
                "create",
                "{{orderId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /payment-splits/process/:orderId",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/payment-splits/process/{{orderId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "payment-splits",
                "process",
                "{{orderId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /payment-splits/verify/:reference",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/payment-splits/verify/{{reference}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "payment-splits",
                "verify",
                "{{reference}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /payment-splits/store/my-splits",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/payment-splits/store/my-splits",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "payment-splits",
                "store",
                "my-splits"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /payment-splits/store/:storeId",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/payment-splits/store/{{storeId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "payment-splits",
                "store",
                "{{storeId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /payment-splits/admin/summary",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/payment-splits/admin/summary",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "payment-splits",
                "admin",
                "summary"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PAYSTACK",
      "item": [
        {
          "name": "POST /paystack/initialize",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/initialize",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "initialize"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /paystack/initialize-split",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/initialize-split",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "initialize-split"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /paystack/verify",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/verify",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "verify"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"reference\": \"{{paymentReference}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack/verify",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/verify",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "verify"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ],
          "response": []
        },
        {
          "name": "POST /paystack/refund",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/refund",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "refund"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /paystack/webhook",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/webhook",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "webhook"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack/public-key",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/public-key",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "public-key"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack/transaction",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/transaction",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "transaction"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack/transactions",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/transactions",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "transactions"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "🧾 Paystack Reconciliation",
          "item": [
            {
              "name": "GET /paystack/reconciliation/orphaned-payments",
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  },
                  {
                    "key": "Authorization",
                    "value": "Bearer {{authToken}}"
                  }
                ],
                "url": {
                  "raw": "{{baseUrl}}/paystack/reconciliation/orphaned-payments",
                  "host": [
                    "{{baseUrl}}"
                  ],
                  "path": [
                    "paystack",
                    "reconciliation",
                    "orphaned-payments"
                  ]
                },
                "description": "Admin-only endpoint for successful online payments that are not linked to valid orders."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "let json = null;",
                      "try {",
                      "  json = pm.response.json();",
                      "} catch (error) {",
                      "  console.log('Response is not valid JSON', error);",
                      "}",
                      "if (json) {",
                      "  const setIf = (key, value) => {",
                      "    if (value !== undefined && value !== null && value !== '') {",
                      "      const normalized = String(value);",
                      "      pm.collectionVariables.set(key, normalized);",
                      "      if (pm.environment) {",
                      "        pm.environment.set(key, normalized);",
                      "      }",
                      "    }",
                      "  };",
                      "  const unsetVar = (key) => {",
                      "    pm.collectionVariables.unset(key);",
                      "    if (pm.environment) {",
                      "      pm.environment.unset(key);",
                      "    }",
                      "  };",
                      "  const decodeJwtPayload = (token) => {",
                      "    try {",
                      "      if (!token || String(token).split('.').length < 2) return null;",
                      "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                      "      while (payload.length % 4) payload += '=';",
                      "      return JSON.parse(atob(payload));",
                      "    } catch (error) {",
                      "      console.log('Unable to decode JWT payload', error);",
                      "      return null;",
                      "    }",
                      "  };",
                      "  const data = json?.data ?? json;",
                      "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                      "  const results = Array.isArray(data?.results) ? data.results : [];",
                      "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                      "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                      "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                      "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                      "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                      "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                      "  const user = data?.user || first?.user || first;",
                      "  const product = first?.product || data?.product || first;",
                      "  const address = first?.address || data?.address || first;",
                      "  const category = first?.category || data?.category || first;",
                      "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                      "  const enquiry = data?.enquiry || first?.enquiry || first;",
                      "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                      "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                      "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                      "  setIf('paymentReference', paystackReference);",
                      "  setIf('reconcileReference', paystackReference);",
                      "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                      "  setIf('paystackStatus', data?.status || json?.status);",
                      "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                      "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                      "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                      "  pm.test('Status code is 200', function () {",
                      "    pm.response.to.have.status(200);",
                      "  });",
                      "  pm.test('Reconciliation has orphanedCount', function () {",
                      "    pm.expect(data).to.have.property('orphanedCount');",
                      "  });",
                      "}"
                    ]
                  }
                }
              ],
              "response": []
            },
            {
              "name": "GET /paystack/reconciliation/orphaned-guest-checkout",
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  },
                  {
                    "key": "Authorization",
                    "value": "Bearer {{authToken}}"
                  }
                ],
                "url": {
                  "raw": "{{baseUrl}}/paystack/reconciliation/orphaned-guest-checkout",
                  "host": [
                    "{{baseUrl}}"
                  ],
                  "path": [
                    "paystack",
                    "reconciliation",
                    "orphaned-guest-checkout"
                  ]
                },
                "description": "Admin-only endpoint to find guest checkouts marked paid but not completed."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "let json = null;",
                      "try {",
                      "  json = pm.response.json();",
                      "} catch (error) {",
                      "  console.log('Response is not valid JSON', error);",
                      "}",
                      "if (json) {",
                      "  const setIf = (key, value) => {",
                      "    if (value !== undefined && value !== null && value !== '') {",
                      "      const normalized = String(value);",
                      "      pm.collectionVariables.set(key, normalized);",
                      "      if (pm.environment) {",
                      "        pm.environment.set(key, normalized);",
                      "      }",
                      "    }",
                      "  };",
                      "  const unsetVar = (key) => {",
                      "    pm.collectionVariables.unset(key);",
                      "    if (pm.environment) {",
                      "      pm.environment.unset(key);",
                      "    }",
                      "  };",
                      "  const decodeJwtPayload = (token) => {",
                      "    try {",
                      "      if (!token || String(token).split('.').length < 2) return null;",
                      "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                      "      while (payload.length % 4) payload += '=';",
                      "      return JSON.parse(atob(payload));",
                      "    } catch (error) {",
                      "      console.log('Unable to decode JWT payload', error);",
                      "      return null;",
                      "    }",
                      "  };",
                      "  const data = json?.data ?? json;",
                      "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                      "  const results = Array.isArray(data?.results) ? data.results : [];",
                      "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                      "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                      "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                      "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                      "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                      "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                      "  const user = data?.user || first?.user || first;",
                      "  const product = first?.product || data?.product || first;",
                      "  const address = first?.address || data?.address || first;",
                      "  const category = first?.category || data?.category || first;",
                      "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                      "  const enquiry = data?.enquiry || first?.enquiry || first;",
                      "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                      "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                      "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                      "  setIf('paymentReference', paystackReference);",
                      "  setIf('reconcileReference', paystackReference);",
                      "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                      "  setIf('paystackStatus', data?.status || json?.status);",
                      "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                      "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                      "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                      "  pm.test('Status code is 200', function () {",
                      "    pm.response.to.have.status(200);",
                      "  });",
                      "  pm.test('Guest reconciliation has unprocessedCheckoutCount', function () {",
                      "    pm.expect(data).to.have.property('unprocessedCheckoutCount');",
                      "  });",
                      "}"
                    ]
                  }
                }
              ],
              "response": []
            },
            {
              "name": "GET /paystack/reconciliation/full-report",
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  },
                  {
                    "key": "Authorization",
                    "value": "Bearer {{authToken}}"
                  }
                ],
                "url": {
                  "raw": "{{baseUrl}}/paystack/reconciliation/full-report?page=1&take=50",
                  "host": [
                    "{{baseUrl}}"
                  ],
                  "path": [
                    "paystack",
                    "reconciliation",
                    "full-report"
                  ],
                  "query": [
                    {
                      "key": "page",
                      "value": "1"
                    },
                    {
                      "key": "take",
                      "value": "50"
                    }
                  ]
                },
                "description": "Admin-only endpoint that returns a consolidated paystack reconciliation report. Supports pagination via page and take query params."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "let json = null;",
                      "try {",
                      "  json = pm.response.json();",
                      "} catch (error) {",
                      "  console.log('Response is not valid JSON', error);",
                      "}",
                      "if (json) {",
                      "  const setIf = (key, value) => {",
                      "    if (value !== undefined && value !== null && value !== '') {",
                      "      const normalized = String(value);",
                      "      pm.collectionVariables.set(key, normalized);",
                      "      if (pm.environment) {",
                      "        pm.environment.set(key, normalized);",
                      "      }",
                      "    }",
                      "  };",
                      "  const unsetVar = (key) => {",
                      "    pm.collectionVariables.unset(key);",
                      "    if (pm.environment) {",
                      "      pm.environment.unset(key);",
                      "    }",
                      "  };",
                      "  const decodeJwtPayload = (token) => {",
                      "    try {",
                      "      if (!token || String(token).split('.').length < 2) return null;",
                      "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                      "      while (payload.length % 4) payload += '=';",
                      "      return JSON.parse(atob(payload));",
                      "    } catch (error) {",
                      "      console.log('Unable to decode JWT payload', error);",
                      "      return null;",
                      "    }",
                      "  };",
                      "  const data = json?.data ?? json;",
                      "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                      "  const results = Array.isArray(data?.results) ? data.results : [];",
                      "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                      "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                      "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                      "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                      "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                      "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                      "  const user = data?.user || first?.user || first;",
                      "  const product = first?.product || data?.product || first;",
                      "  const address = first?.address || data?.address || first;",
                      "  const category = first?.category || data?.category || first;",
                      "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                      "  const enquiry = data?.enquiry || first?.enquiry || first;",
                      "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                      "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                      "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                      "  setIf('paymentReference', paystackReference);",
                      "  setIf('reconcileReference', paystackReference);",
                      "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                      "  setIf('paystackStatus', data?.status || json?.status);",
                      "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                      "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                      "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                      "  pm.test('Status code is 200', function () {",
                      "    pm.response.to.have.status(200);",
                      "  });",
                      "  pm.test('Full report has summary', function () {",
                      "    pm.expect(data).to.have.property('summary');",
                      "  });",
                      "  pm.test('Full report has orderDetails', function () {",
                      "    pm.expect(data).to.have.property('details');",
                      "    pm.expect(data.details).to.have.property('orderDetails');",
                      "  });",
                      "}"
                    ]
                  }
                }
              ],
              "response": []
            },
            {
              "name": "GET /paystack/reconciliation/audit-mismatches",
              "request": {
                "method": "GET",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  },
                  {
                    "key": "Authorization",
                    "value": "Bearer {{authToken}}"
                  }
                ],
                "url": {
                  "raw": "{{baseUrl}}/paystack/reconciliation/audit-mismatches",
                  "host": [
                    "{{baseUrl}}"
                  ],
                  "path": [
                    "paystack",
                    "reconciliation",
                    "audit-mismatches"
                  ]
                },
                "description": "Admin-only endpoint to audit mismatches between ORDER and ORDER_PAYMENTS records."
              },
              "event": [
                {
                  "listen": "test",
                  "script": {
                    "type": "text/javascript",
                    "exec": [
                      "let json = null;",
                      "try {",
                      "  json = pm.response.json();",
                      "} catch (error) {",
                      "  console.log('Response is not valid JSON', error);",
                      "}",
                      "if (json) {",
                      "  const setIf = (key, value) => {",
                      "    if (value !== undefined && value !== null && value !== '') {",
                      "      const normalized = String(value);",
                      "      pm.collectionVariables.set(key, normalized);",
                      "      if (pm.environment) {",
                      "        pm.environment.set(key, normalized);",
                      "      }",
                      "    }",
                      "  };",
                      "  const unsetVar = (key) => {",
                      "    pm.collectionVariables.unset(key);",
                      "    if (pm.environment) {",
                      "      pm.environment.unset(key);",
                      "    }",
                      "  };",
                      "  const decodeJwtPayload = (token) => {",
                      "    try {",
                      "      if (!token || String(token).split('.').length < 2) return null;",
                      "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                      "      while (payload.length % 4) payload += '=';",
                      "      return JSON.parse(atob(payload));",
                      "    } catch (error) {",
                      "      console.log('Unable to decode JWT payload', error);",
                      "      return null;",
                      "    }",
                      "  };",
                      "  const data = json?.data ?? json;",
                      "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                      "  const results = Array.isArray(data?.results) ? data.results : [];",
                      "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                      "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                      "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                      "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                      "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                      "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                      "  const user = data?.user || first?.user || first;",
                      "  const product = first?.product || data?.product || first;",
                      "  const address = first?.address || data?.address || first;",
                      "  const category = first?.category || data?.category || first;",
                      "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                      "  const enquiry = data?.enquiry || first?.enquiry || first;",
                      "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                      "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                      "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                      "  setIf('paymentReference', paystackReference);",
                      "  setIf('reconcileReference', paystackReference);",
                      "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                      "  setIf('paystackStatus', data?.status || json?.status);",
                      "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                      "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                      "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                      "  pm.test('Status code is 200', function () {",
                      "    pm.response.to.have.status(200);",
                      "  });",
                      "  pm.test('Audit response has summary', function () {",
                      "    pm.expect(data).to.have.property('summary');",
                      "  });",
                      "}"
                    ]
                  }
                }
              ],
              "response": []
            }
          ]
        },
        {
          "name": "GET /paystack/success",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/success",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "success"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack/cancel",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/cancel",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "cancel"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack/failed",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/failed",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "failed"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /paystack/transactions/reconcile",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"dryRun\": true,\n  \"status\": \"success\",\n  \"page\": 1,\n  \"perPage\": 50,\n  \"maxPages\": 1\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/paystack/transactions/reconcile",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "transactions",
                "reconcile"
              ]
            },
            "description": "Admin-only reconciliation for historical Paystack transactions. Run with dryRun=true first, then set dryRun=false to replay reconcilable entries through the existing webhook sync flow."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('paymentReference', reconcile?.reference);",
                  "  setIf('reconcileReference', reconcile?.reference);",
                  "  setIf('paystackStatus', reconcile?.paystack_status);",
                  "  setIf('paystackCustomerEmail', reconcile?.customer_email);",
                  "  setIf('paystackAmountKobo', reconcile?.amount_kobo);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /paystack/initialize-checkout",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/initialize-checkout",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "initialize-checkout"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"payment_provider\": \"{{paymentProvider}}\",\n  \"order_payload\": {\n    \"cart\": [\n      {\n        \"id\": \"{{cartItemId}}\",\n        \"productId\": \"{{productId}}\",\n        \"variantId\": \"{{variantId}}\",\n        \"storeId\": \"{{storeId}}\",\n        \"quantity\": \"{{orderQuantity}}\"\n      }\n    ],\n    \"payment\": {\n      \"type\": \"{{paymentProvider}}\",\n      \"callback_url\": \"{{callbackUrl}}\"\n    },\n    \"address\": {\n      \"id\": \"{{addressId}}\"\n    },\n    \"charges\": {\n      \"token\": \"{{deliveryToken}}\"\n    }\n  },\n  \"callback_url\": \"{{callbackUrl}}\"\n}"
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /paystack/initialize-guest",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/initialize-guest",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "initialize-guest"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"payment_provider\": \"{{paymentProvider}}\",\n  \"guest_info\": {\n    \"email\": \"{{guestEmail}}\",\n    \"first_name\": \"Guest\",\n    \"last_name\": \"Buyer\",\n    \"phone\": \"08000000000\"\n  },\n  \"cart_items\": [\n    {\n      \"product_id\": \"{{productId}}\",\n      \"store_id\": \"{{storeId}}\",\n      \"quantity\": 1,\n      \"unit_price\": 500000\n    }\n  ],\n  \"amount\": 500000,\n  \"delivery_charge\": 0,\n  \"currency\": \"NGN\",\n  \"callback_url\": \"{{callbackUrl}}\",\n  \"order_payload\": {\n    \"guest_info\": {\n      \"email\": \"{{guestEmail}}\",\n      \"first_name\": \"Guest\",\n      \"last_name\": \"Buyer\",\n      \"phone\": \"08000000000\"\n    },\n    \"delivery_address\": {\n      \"id\": \"guest_address_001\",\n      \"full_name\": \"Guest Buyer\",\n      \"phone_no\": \"08000000000\",\n      \"full_address\": \"12 Test Street, Ikeja, Lagos\",\n      \"city\": \"Ikeja\",\n      \"state\": \"Lagos\",\n      \"state_id\": \"{{stateId}}\",\n      \"country\": \"Nigeria\",\n      \"country_id\": \"{{countryId}}\"\n    },\n    \"cart_items\": [\n      {\n        \"product_id\": \"{{productId}}\",\n        \"store_id\": \"{{storeId}}\",\n        \"product_name\": \"{{productName}}\",\n        \"quantity\": 1,\n        \"unit_price\": 500000,\n        \"total_price\": 500000\n      }\n    ],\n    \"payment\": {\n      \"payment_method\": \"{{paymentProvider}}\",\n      \"payment_status\": \"pending\"\n    },\n    \"delivery\": {\n      \"delivery_token\": \"{{deliveryToken}}\"\n    },\n    \"order_summary\": {\n      \"subtotal\": 5000,\n      \"delivery_fee\": 0,\n      \"discount\": 0,\n      \"tax\": 0,\n      \"total\": 5000\n    }\n  }\n}"
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /paystack/verify-guest",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/verify-guest",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "verify-guest"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /paystack/transactions/diagnose",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/transactions/diagnose",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "transactions",
                "diagnose"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack/settlement-audit",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/settlement-audit",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "settlement-audit"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack/manual-settlement/audit",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/manual-settlement/audit?page=1&take=20&storeId={{storeId}}&reference={{reconcileReference}}&buyerEmail={{buyerEmail}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "manual-settlement",
                "audit"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1"
                },
                {
                  "key": "take",
                  "value": "20"
                },
                {
                  "key": "storeId",
                  "value": "{{storeId}}"
                },
                {
                  "key": "reference",
                  "value": "{{reconcileReference}}"
                },
                {
                  "key": "buyerEmail",
                  "value": "{{buyerEmail}}"
                }
              ]
            },
            "description": "Admin-only audit endpoint for all non-split payments, including company-account collections and legacy payments without split metadata. Supports filtering by storeId, reference, and buyerEmail."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /admin/paystack/subaccounts/update-percentage",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"percentage_charge\": 93.5,\n  \"dryRun\": true\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/admin/paystack/subaccounts/update-percentage",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "admin",
                "paystack",
                "subaccounts",
                "update-percentage"
              ]
            },
            "description": "Admin-only bulk updater for new-account Paystack subaccounts. Omit storeIds to update every migrated store, or pass storeIds to target specific stores. Use dryRun=true first to preview affected stores, then set dryRun=false to push the update to Paystack and sync local store records."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /admin/paystack/subaccounts/sync-new-codes",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"dryRun\": true,\n  \"perPage\": 100\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/admin/paystack/subaccounts/sync-new-codes",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "admin",
                "paystack",
                "subaccounts",
                "sync-new-codes"
              ]
            },
            "description": "Admin-only backfill for copied Paystack subaccounts. It fetches subaccounts from the new Paystack account, matches them to local stores, and stores paystack_subaccount_code_new locally. Use dryRun=true first."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /admin/paystack/subaccounts/unmatched-remote",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/admin/paystack/subaccounts/unmatched-remote?page=1&limit=100",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "admin",
                "paystack",
                "subaccounts",
                "unmatched-remote"
              ],
              "query": [
                {
                  "key": "page",
                  "value": "1"
                },
                {
                  "key": "limit",
                  "value": "100"
                }
              ]
            },
            "description": "Admin-only list of live new-account Paystack subaccounts that are not currently matched to local stores targeted by the percentage update flow. Each item includes a classification, candidate_store_ids, and a reason."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /admin/paystack/subaccounts/resolve-unmatched",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"dryRun\": true,\n  \"classifications\": [\n    \"missing_local_link\"\n  ],\n  \"page\": 1,\n  \"limit\": 100\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/admin/paystack/subaccounts/resolve-unmatched",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "admin",
                "paystack",
                "subaccounts",
                "resolve-unmatched"
              ]
            },
            "description": "Admin-only safe auto-linker for unmatched remote Paystack subaccounts. By default it previews only missing_local_link records with exactly one candidate store. Set dryRun=false after verifying the preview."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PAYSTACK SUBACCOUNTS",
      "item": [
        {
          "name": "POST /paystack-subaccounts/request",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack-subaccounts/request",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack-subaccounts",
                "request"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack-subaccounts/my-subaccount",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack-subaccounts/my-subaccount",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack-subaccounts",
                "my-subaccount"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack-subaccounts/pending",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack-subaccounts/pending",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack-subaccounts",
                "pending"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /admin/paystack/subaccounts/migrate",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/admin/paystack/subaccounts/migrate?dryRun=true&force=false",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "admin",
                "paystack",
                "subaccounts",
                "migrate?dryRun=true&force=false"
              ],
              "query": [
                {
                  "key": "dryRun",
                  "value": "true"
                },
                {
                  "key": "force",
                  "value": "false"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /admin/paystack/subaccounts/migrate/:storeId",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/admin/paystack/subaccounts/migrate/{{storeId}}?dryRun=true&force=false",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "admin",
                "paystack",
                "subaccounts",
                "migrate",
                "{{storeId}}?dryRun=true&force=false"
              ],
              "query": [
                {
                  "key": "dryRun",
                  "value": "true"
                },
                {
                  "key": "force",
                  "value": "false"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /admin/paystack/subaccounts/migrate/retry-failed",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/admin/paystack/subaccounts/migrate/retry-failed?dryRun=true",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "admin",
                "paystack",
                "subaccounts",
                "migrate",
                "retry-failed?dryRun=true"
              ],
              "query": [
                {
                  "key": "dryRun",
                  "value": "true"
                }
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /admin/paystack/subaccounts/migration-status",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/admin/paystack/subaccounts/migration-status?status=&page=1&limit=50",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "admin",
                "paystack",
                "subaccounts",
                "migration-status?status=&page=1&limit=50"
              ],
              "query": [
                {
                  "key": "status",
                  "value": ""
                },
                {
                  "key": "page",
                  "value": "1"
                },
                {
                  "key": "limit",
                  "value": "50"
                }
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /admin/paystack/subaccounts/migration-status/report.csv",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "text/csv"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/admin/paystack/subaccounts/migration-status/report.csv?status=",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "admin",
                "paystack",
                "subaccounts",
                "migration-status",
                "report.csv?status="
              ],
              "query": [
                {
                  "key": "status",
                  "value": ""
                }
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /paystack-subaccounts/:id/approve",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack-subaccounts/{{storeId}}/approve",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack-subaccounts",
                "{{storeId}}",
                "approve"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /paystack-subaccounts/:id/reject",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack-subaccounts/{{storeId}}/reject",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack-subaccounts",
                "{{storeId}}",
                "reject"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack-subaccounts/banks/supported",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack-subaccounts/banks/supported",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack-subaccounts",
                "banks",
                "supported"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack-subaccounts/banks/validate",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack-subaccounts/banks/validate",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack-subaccounts",
                "banks",
                "validate"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack-subaccounts/banks/:bankCode/supported",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack-subaccounts/banks/{{bankCode}}/supported",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack-subaccounts",
                "banks",
                "{{bankCode}}",
                "supported"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /paystack-subaccounts/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack-subaccounts/{{storeId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack-subaccounts",
                "{{storeId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PRINT",
      "item": [
        {
          "name": "GET /print/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /print/store",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/store",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "store"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /print/getall",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/getall",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "getall"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /print/buy_again",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/buy_again",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "buy_again"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /print/store/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/store/{{orderId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "store",
                "{{orderId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /print/user/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/user/{{orderId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "user",
                "{{orderId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /print/details/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/details/{{orderId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "details",
                "{{orderId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /print/get_one/admin/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/get_one/admin/{{orderId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "get_one",
                "admin",
                "{{orderId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /print/get_one/seller/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/get_one/seller/{{orderId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "get_one",
                "seller",
                "{{orderId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /print/get_one/user/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/get_one/user/{{orderId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "get_one",
                "user",
                "{{orderId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /print",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /print/update_status/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/update_status/{{orderId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "update_status",
                "{{orderId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"status\": \"processing\",\n  \"remark\": \"Order is being processed\",\n  \"delivery_date\": \"2026-04-10T00:00:00.000Z\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /print/update_payment/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/update_payment/{{orderId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "update_payment",
                "{{orderId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /print/cancel_order/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/print/cancel_order/{{orderId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "print",
                "cancel_order",
                "{{orderId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PRINT CONFIGERATION",
      "item": [
        {
          "name": "POST /Print_Configeration/bulk",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/Print_Configeration/bulk",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "Print_Configeration",
                "bulk"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /Print_Configeration",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/Print_Configeration",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "Print_Configeration"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /Print_Configeration/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/Print_Configeration/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "Print_Configeration",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /Print_Configeration/price",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/Print_Configeration/price",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "Print_Configeration",
                "price"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /Print_Configeration/configurations",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/Print_Configeration/configurations",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "Print_Configeration",
                "configurations"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /Print_Configeration/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/Print_Configeration/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "Print_Configeration",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PRINTSTATUS",
      "item": [
        {
          "name": "GET /printStatus/all/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/printStatus/all/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "printStatus",
                "all",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PRODUCTS",
      "item": [
        {
          "name": "GET /products/bystore",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/bystore",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "bystore"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /products/bystore/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/bystore/{{productId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "bystore",
                "{{productId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /products/seller/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/seller/{{productId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "seller",
                "{{productId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /products/create",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/create",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "create"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"title\": \"Sample Product\",\n  \"description\": \"A short description\",\n  \"price\": 2500,\n  \"stock\": 10,\n  \"categoryId\": 1\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /products/upload/",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/upload",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "upload"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"title\": \"Sample Product\",\n  \"description\": \"A short description\",\n  \"price\": 2500,\n  \"stock\": 10,\n  \"categoryId\": 1\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /products/upload/files",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/upload/files",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "upload",
                "files"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"title\": \"Sample Product\",\n  \"description\": \"A short description\",\n  \"price\": 2500,\n  \"stock\": 10,\n  \"categoryId\": 1\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /products/update/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/update/{{productId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "update",
                "{{productId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"title\": \"Sample Product\",\n  \"description\": \"A short description\",\n  \"price\": 2500,\n  \"stock\": 10,\n  \"categoryId\": 1\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /products/update_image/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/update_image/{{productId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "update_image",
                "{{productId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"title\": \"Sample Product\",\n  \"description\": \"A short description\",\n  \"price\": 2500,\n  \"stock\": 10,\n  \"categoryId\": 1\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /products/update_cover_img/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/update_cover_img/{{productId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "update_cover_img",
                "{{productId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"title\": \"Sample Product\",\n  \"description\": \"A short description\",\n  \"price\": 2500,\n  \"stock\": 10,\n  \"categoryId\": 1\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /products/update_status/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/update_status/{{productId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "update_status",
                "{{productId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"status\": true\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /products/update/variant/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/update/variant/{{productId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "update",
                "variant",
                "{{productId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"title\": \"Sample Product\",\n  \"description\": \"A short description\",\n  \"price\": 2500,\n  \"stock\": 10,\n  \"categoryId\": 1\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /products/delete/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/delete/{{productId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "delete",
                "{{productId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /products/delete_image/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/delete_image/{{productId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "delete_image",
                "{{productId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /products/check-same-store",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products/check-same-store",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "products",
                "check-same-store"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"title\": \"Sample Product\",\n  \"description\": \"A short description\",\n  \"price\": 2500,\n  \"stock\": 10,\n  \"categoryId\": 1\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PRODUCTIMAGE",
      "item": [
        {
          "name": "PUT /productimage/update/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/productimage/update/{{productId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "productimage",
                "update",
                "{{productId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"title\": \"Sample Product\",\n  \"description\": \"A short description\",\n  \"price\": 2500,\n  \"stock\": 10,\n  \"categoryId\": 1\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PRODUCTSREVIEWS",
      "item": [
        {
          "name": "GET /productsReviews/review",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/productsReviews/review",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "productsReviews",
                "review"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /productsReviews",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/productsReviews",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "productsReviews"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"title\": \"Sample Product\",\n  \"description\": \"A short description\",\n  \"price\": 2500,\n  \"stock\": 10,\n  \"categoryId\": 1\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /productsReviews/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/productsReviews/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "productsReviews",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PRODUCT SEARCH",
      "item": [
        {
          "name": "GET /product_search/multi",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/product_search/multi",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "product_search",
                "multi"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /product_search/single",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/product_search/single",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "product_search",
                "single"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /product_search/store_top",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/product_search/store_top",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "product_search",
                "store_top"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /product_search/print/available",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/product_search/print/available",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "product_search",
                "print",
                "available"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /product_search/details/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/product_search/details/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "product_search",
                "details",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /product_search/autocomplete",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/product_search/autocomplete",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "product_search",
                "autocomplete"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /product_search/store/:slug",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/product_search/store/{{slug}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "product_search",
                "store",
                "{{slug}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /product_search/store_items/:slug",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/product_search/store_items/{{slug}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "product_search",
                "store_items",
                "{{slug}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /product_search/store_items/category/products",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/product_search/store_items/category/products",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "product_search",
                "store_items",
                "category",
                "products"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /product_search/boosted-category",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/product_search/boosted-category",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "product_search",
                "boosted-category"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /product_search/:slug",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/product_search/{{slug}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "product_search",
                "{{slug}}"
              ],
              "variable": [
                {
                  "key": "slug",
                  "value": "{{productSlug}}"
                }
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('productId', product?._id || product?.id || product?.productId || data?._id || data?.id);",
                  "  setIf('productPid', product?.pid || product?._id || product?.id);",
                  "  setIf('storeId', product?.store_id || product?.storeId || store?.id || data?.store_id || data?.storeId);",
                  "  setIf('productName', product?.name || product?.title || data?.name || data?.title);",
                  "  setIf('productPrice', product?.price || product?.retail_rate || product?.retailRate || data?.price || data?.retail_rate);",
                  "  setIf('productWeight', product?.product_weight || product?.productWeight || product?.weight || data?.product_weight || data?.productWeight || data?.weight);",
                  "  setIf('storeSlug', store?.slug || first?.slug || data?.slug);",
                  "  setIf('categoryId', product?.category || product?.categoryId || category?.id || category?._id || data?.category || data?.categoryId);",
                  "  setIf('subCategoryId', product?.subCategory || product?.subCategoryId || subCategory?.id || subCategory?._id || data?.subCategory || data?.subCategoryId);",
                  "  setIf('variantId', firstVariant?.id || firstVariant?._id || product?.variantId || first?.variantId);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PRODUCTVARIANT",
      "item": [
        {
          "name": "POST /productvariant/add_variants",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/productvariant/add_variants",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "productvariant",
                "add_variants"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"title\": \"Sample Product\",\n  \"description\": \"A short description\",\n  \"price\": 2500,\n  \"stock\": 10,\n  \"categoryId\": 1\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /productvariant/delete/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/productvariant/delete/{{variantId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "productvariant",
                "delete",
                "{{variantId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "REFUND REQUEST",
      "item": [
        {
          "name": "GET /refund_request/admin/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/refund_request/admin/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "refund_request",
                "admin",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /refund_request/store/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/refund_request/store/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "refund_request",
                "store",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /refund_request/user/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/refund_request/user/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "refund_request",
                "user",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /refund_request/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/refund_request/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "refund_request",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /refund_request/admin/approve/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/refund_request/admin/approve/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "refund_request",
                "admin",
                "approve",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "SETTINGS",
      "item": [
        {
          "name": "GET /settings",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settings",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settings"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /settings/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settings/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settings",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "SETTLEMENTS",
      "item": [
        {
          "name": "GET /settlements/summary",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/summary",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "summary"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /settlements/history",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/history",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "history"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /settlements/historyById/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/historyById/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "historyById",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /settlements/allSettlementsByStore",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/allSettlementsByStore",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "allSettlementsByStore"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /settlements/allSettlementsByAdmin",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/allSettlementsByAdmin",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "allSettlementsByAdmin"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /settlements/summary/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/summary/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "summary",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /settlements/history/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/history/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "history",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /settlements/details/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/details/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "details",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /settlements/store/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/store/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "store",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /settlements/update/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/update/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "update",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /settlements/updateStatus/:id/:type",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/updateStatus/{{id}}/{{type}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "updateStatus",
                "{{id}}",
                "{{type}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /settlements/create",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/create",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "create"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /settlements/createByStore",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/settlements/createByStore",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "settlements",
                "createByStore"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "STATES",
      "item": [
        {
          "name": "GET /states",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/states",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "states"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /states",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/states",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "states"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /states/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/states/{{stateId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "states",
                "{{stateId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /states/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/states/{{stateId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "states",
                "{{stateId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "COORPORATE STORE",
      "item": [
        {
          "name": "GET /coorporate_store/store_check",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/store_check",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "store_check"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /coorporate_store/account/details",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/account/details",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "account",
                "details"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /coorporate_store/account/details",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/account/details",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "account",
                "details"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /coorporate_store/details",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/details",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "details"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /coorporate_store/dashboard/admin",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/dashboard/admin",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "dashboard",
                "admin"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /coorporate_store/sellerdetails/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/sellerdetails/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "sellerdetails",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /coorporate_store/pgn/:type",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/pgn/{{type}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "pgn",
                "{{type}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /coorporate_store/dashboard/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/dashboard/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "dashboard",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /coorporate_store/print/time/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/print/time/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "print",
                "time",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /coorporate_store/create",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/create",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "create"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /coorporate_store/createexist",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/createexist",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "createexist"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /coorporate_store/send_mail",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/send_mail",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "send_mail"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /coorporate_store/update",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/update",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "update"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /coorporate_store/status/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/status/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "status",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /coorporate_store/deactivate/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/deactivate/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "deactivate",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /coorporate_store/slug/:slug",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/coorporate_store/slug/{{slug}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "coorporate_store",
                "slug",
                "{{slug}}"
              ],
              "variable": [
                {
                  "key": "slug",
                  "value": "{{storeSlug}}"
                }
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  setIf('storeId', store?.id || store?._id || data?.id || data?._id);",
                  "  setIf('storeSlug', store?.slug || data?.slug || first?.slug);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "STOREREVIEW",
      "item": [
        {
          "name": "POST /storereview/create",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/storereview/create",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "storereview",
                "create"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "STORE SEARCH",
      "item": [
        {
          "name": "GET /store_search/info",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/store_search/info",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "store_search",
                "info"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /store_search/info/:slug",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/store_search/info/{{storeSlug}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "store_search",
                "info",
                "{{storeSlug}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /store_search/info/category/store",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/store_search/info/category/store",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "store_search",
                "info",
                "category",
                "store"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /store_search/banner/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/store_search/banner/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "store_search",
                "banner",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "SUBSCRIPTION PLANS",
      "item": [
        {
          "name": "GET /subscription-plans",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subscription-plans",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subscription-plans"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /subscription-plans/active",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subscription-plans/active",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subscription-plans",
                "active"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /subscription-plans/public/active",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subscription-plans/public/active",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subscription-plans",
                "public",
                "active"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /subscription-plans/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subscription-plans/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subscription-plans",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /subscription-plans",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subscription-plans",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subscription-plans"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /subscription-plans",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subscription-plans",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subscription-plans"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /subscription-plans/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subscription-plans/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subscription-plans",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /subscription-plans/bulk-delete",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subscription-plans/bulk-delete",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subscription-plans",
                "bulk-delete"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /subscription-plans/featured-position",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subscription-plans/featured-position",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subscription-plans",
                "featured-position"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "SUBTITUTION TOKEN",
      "item": [
        {
          "name": "POST /subtitution/token",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subtitution/token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subtitution",
                "token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "SUBCATEGORY",
      "item": [
        {
          "name": "GET /subCategory/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subCategory/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subCategory",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /subCategory",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subCategory",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subCategory"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /subCategory/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subCategory/{{subCategoryId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subCategory",
                "{{subCategoryId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /subCategory/position/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subCategory/position/{{subCategoryId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subCategory",
                "position",
                "{{subCategoryId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /subCategory/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/subCategory/{{subCategoryId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "subCategory",
                "{{subCategoryId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "USER",
      "item": [
        {
          "name": "GET /user",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /user/refresh-user",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user/refresh-user",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user",
                "refresh-user"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /user/details/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user/details/{{userId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user",
                "details",
                "{{userId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /user/check_user/validate",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user/check_user/validate",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user",
                "check_user",
                "validate"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Jane Customer\",\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\",\n  \"phone\": \"+2348012345678\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /user/deactivate",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user/deactivate",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user",
                "deactivate"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Jane Customer\",\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\",\n  \"phone\": \"+2348012345678\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /user/update-name",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user/update-name",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user",
                "update-name"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Jane Customer\",\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\",\n  \"phone\": \"+2348012345678\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /user/update-password",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user/update-password",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user",
                "update-password"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Jane Customer\",\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\",\n  \"phone\": \"+2348012345678\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /user/add-password",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user/add-password",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user",
                "add-password"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Jane Customer\",\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\",\n  \"phone\": \"+2348012345678\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /user/update-email",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user/update-email",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user",
                "update-email"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Jane Customer\",\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\",\n  \"phone\": \"+2348012345678\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /user/update-photo",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user/update-photo",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user",
                "update-photo"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Jane Customer\",\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\",\n  \"phone\": \"+2348012345678\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /user/update-Phone",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user/update-Phone",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user",
                "update-Phone"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Jane Customer\",\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\",\n  \"phone\": \"+2348012345678\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /user/reactivate/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user/reactivate/{{userId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user",
                "reactivate",
                "{{userId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Jane Customer\",\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\",\n  \"phone\": \"+2348012345678\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /user/me",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user",
                "me"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "AUTH",
      "item": [
        {
          "name": "GET /auth/signout",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/signout",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "signout"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /auth/signoutall",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/signoutall",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "signoutall"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /auth/checkphone/:phone",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/checkphone/{{phone}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "checkphone",
                "{{phone}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /auth/checkEmail/:email",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/checkEmail/{{userEmail}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "checkEmail",
                "{{userEmail}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/signup",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/signup",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "signup"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/email-verify",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/email-verify",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "email-verify"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/verify-email",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/verify-email",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "verify-email"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/refresh-token",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/refresh-token",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "refresh-token"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"{{userEmail}}\",\n  \"password\": \"{{password}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PATCH /auth/change-password",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "description": "Logged-in admin or marketplace user changes their password using oldPassword, newPassword, and confirmPassword.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"oldPassword\": \"{{oldPassword}}\",\n  \"newPassword\": \"{{newPassword}}\",\n  \"confirmPassword\": \"{{newPassword}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/auth/change-password",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "change-password"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/forgot-password",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "description": "Unified forgot-password endpoint for admin and marketplace users. Always returns: If this email exists, a reset code has been sent.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"{{userEmail}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/auth/forgot-password",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "forgot-password"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/reset-password",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "description": "Unified reset-password endpoint for admin and marketplace users. Requires email, reset token, newPassword, and confirmPassword.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"{{userEmail}}\",\n  \"token\": \"{{resetToken}}\",\n  \"newPassword\": \"{{newPassword}}\",\n  \"confirmPassword\": \"{{newPassword}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/auth/reset-password",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "reset-password"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/phone-login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/phone-login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "phone-login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/google-login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/google-login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "google-login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/apple-login",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/apple-login",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "apple-login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/forgot-password",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/forgot-password",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "forgot-password"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"P@ssw0rd\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/reset-password",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/reset-password",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "reset-password"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"P@ssw0rd\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/request-deactivate",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/request-deactivate",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "request-deactivate"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /auth/deactivate-account",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/auth/deactivate-account",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "auth",
                "deactivate-account"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "USER BANK ACCOUNT",
      "item": [
        {
          "name": "GET /user-bank-account/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user-bank-account/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user-bank-account",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /user-bank-account/all/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user-bank-account/all/{{bankAccountId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user-bank-account",
                "all",
                "{{bankAccountId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /user-bank-account",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user-bank-account",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user-bank-account"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Jane Customer\",\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\",\n  \"phone\": \"+2348012345678\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /user-bank-account/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user-bank-account/{{bankAccountId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user-bank-account",
                "{{bankAccountId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Jane Customer\",\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\",\n  \"phone\": \"+2348012345678\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /user-bank-account/soft/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user-bank-account/soft/{{bankAccountId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user-bank-account",
                "soft",
                "{{bankAccountId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /user-bank-account",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user-bank-account",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user-bank-account"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /user-bank-account/:id",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user-bank-account/{{bankAccountId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user-bank-account",
                "{{bankAccountId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /user-bank-account/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user-bank-account/{{bankAccountId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user-bank-account",
                "{{bankAccountId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Jane Customer\",\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\",\n  \"phone\": \"+2348012345678\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /user-bank-account/setdefault/:id",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user-bank-account/setdefault/{{bankAccountId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user-bank-account",
                "setdefault",
                "{{bankAccountId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Jane Customer\",\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\",\n  \"phone\": \"+2348012345678\"\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /user-bank-account/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/user-bank-account/{{bankAccountId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "user-bank-account",
                "{{bankAccountId}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "USERHISTORY",
      "item": [
        {
          "name": "GET /userhistory/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/userhistory/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "userhistory",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "WISHLIST",
      "item": [
        {
          "name": "GET /wishlist/all",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/wishlist/all",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "wishlist",
                "all"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /wishlist",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/wishlist",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "wishlist"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"userId\": 1,\n  \"productId\": 123\n}"
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /wishlist/:id",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/wishlist/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "wishlist",
                "{{id}}"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "NEWSANDBLOGS",
      "item": [
        {
          "name": "GET /newsandblogs",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/newsandblogs",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "newsandblogs"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /newsandblogs/{{id}}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/newsandblogs/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "newsandblogs",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /newsandblogs",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/newsandblogs",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "newsandblogs"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PUT /newsandblogs/{{id}}",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/newsandblogs/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "newsandblogs",
                "{{id}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /newsandblogs/{{id}}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/newsandblogs/{{id}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "newsandblogs",
                "{{id}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "PAYSTACK RECONCILIATION",
      "item": [
        {
          "name": "GET /paystack/reconciliation/full-report",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/paystack/reconciliation/full-report",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "paystack",
                "reconciliation",
                "full-report"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const paystackReference = data?.reference || json?.reference || payment?.ref;",
                  "  setIf('paymentReference', paystackReference);",
                  "  setIf('reconcileReference', paystackReference);",
                  "  setIf('paystackAuthUrl', data?.authorization_url || json?.authorization_url);",
                  "  setIf('paystackStatus', data?.status || json?.status);",
                  "  setIf('paystackCustomerEmail', data?.customer?.email || data?.authorization?.email);",
                  "  setIf('paystackAmountKobo', data?.amount || json?.amount);",
                  "  setIf('guestEmail', data?.customer?.email || data?.authorization?.email || first?.guest_email);",
                  "  pm.test('Status code is 200', function () {",
                  "    pm.response.to.have.status(200);",
                  "  });",
                  "  pm.test('Full report has summary', function () {",
                  "    pm.expect(data).to.have.property('summary');",
                  "  });",
                  "  pm.test('Full report has orderDetails', function () {",
                  "    pm.expect(data).to.have.property('details');",
                  "    pm.expect(data.details).to.have.property('orderDetails');",
                  "  });",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "ADMIN PAYSTACK SUBACCOUNTS",
      "item": [
        {
          "name": "POST /admin/paystack/subaccounts/migrate",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/admin/paystack/subaccounts/migrate",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "admin",
                "paystack",
                "subaccounts",
                "migrate"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /admin/paystack/subaccounts/migrate/retry-failed",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/admin/paystack/subaccounts/migrate/retry-failed",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "admin",
                "paystack",
                "subaccounts",
                "migrate",
                "retry-failed"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /admin/paystack/subaccounts/migrate/{{storeId}}",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/admin/paystack/subaccounts/migrate/{{storeId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "admin",
                "paystack",
                "subaccounts",
                "migrate",
                "{{storeId}}"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /admin/paystack/subaccounts/migration-status",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/admin/paystack/subaccounts/migration-status",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "admin",
                "paystack",
                "subaccounts",
                "migration-status"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /admin/paystack/subaccounts/migration-status/report.csv",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/admin/paystack/subaccounts/migration-status/report.csv",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "admin",
                "paystack",
                "subaccounts",
                "migration-status",
                "report.csv"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "USERS",
      "item": [
        {
          "name": "POST /users/upgrade-to-seller",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/users/upgrade-to-seller",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "users",
                "upgrade-to-seller"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Jane Customer\",\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\",\n  \"phone\": \"+2348012345678\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /users/downgrade-to-buyer",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/users/downgrade-to-buyer",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "users",
                "downgrade-to-buyer"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Jane Customer\",\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\",\n  \"phone\": \"+2348012345678\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PATCH /users/me/active-role",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/users/me/active-role",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "users",
                "me",
                "active-role"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Jane Customer\",\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\",\n  \"phone\": \"+2348012345678\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PATCH /users/{{managedUserId}}/disable",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/users/{{managedUserId}}/disable",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "users",
                "{{managedUserId}}",
                "disable"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Jane Customer\",\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\",\n  \"phone\": \"+2348012345678\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PATCH /users/{{managedUserId}}/enable",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/users/{{managedUserId}}/enable",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "users",
                "{{managedUserId}}",
                "enable"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Jane Customer\",\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\",\n  \"phone\": \"+2348012345678\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /users/{{managedUserId}}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/users/{{managedUserId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "users",
                "{{managedUserId}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /users/admin-invite",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/users/admin-invite",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "users",
                "admin-invite"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Jane Customer\",\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\",\n  \"phone\": \"+2348012345678\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /users/admin-invite/accept",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/users/admin-invite/accept",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "users",
                "admin-invite",
                "accept"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Jane Customer\",\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\",\n  \"phone\": \"+2348012345678\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "PATCH /users/{{managedUserId}}/assign-admin",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/users/{{managedUserId}}/assign-admin",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "users",
                "{{managedUserId}}",
                "assign-admin"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Jane Customer\",\n  \"email\": \"olagiddz@gmail.com\",\n  \"password\": \"Options123#\",\n  \"phone\": \"+2348012345678\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "DELETE /users/me",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/users/me",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "users",
                "me"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const resolvedAuthToken = json?.token || data?.token || json?.accessToken || data?.accessToken || json?.access_token || data?.access_token || json?.jwt || data?.jwt || data?.access?.token || json?.access?.token;",
                  "  const resolvedRefreshToken = json?.refreshToken || data?.refreshToken || json?.refresh_token || data?.refresh_token || data?.refresh?.token || json?.refresh?.token;",
                  "  setIf('authToken', resolvedAuthToken);",
                  "  setIf('refreshToken', resolvedRefreshToken);",
                  "  setIf('userId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('managedUserId', user?._id || user?.id || data?._id || data?.id);",
                  "  setIf('userEmail', user?.email || data?.email || data?.profile?.email || json?.profile?.email);",
                  "  setIf('storeId', user?.store_id || user?.storeId || data?.store_id || data?.storeId);",
                  "  setIf('activeRole', user?.active_role || user?.role || data?.active_role || data?.role || data?.activeRole || user?.activeRole);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "MAIL",
      "item": [
        {
          "name": "GET /mail/logs",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/mail/logs",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "mail",
                "logs"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "MARKETPLACE FEED",
      "item": [
        {
          "name": "GET /marketplace-feed/home",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/marketplace-feed/home",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "marketplace-feed",
                "home"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /marketplace-feed/products",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/marketplace-feed/products",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "marketplace-feed",
                "products"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /marketplace-feed/products/search",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/marketplace-feed/products/search",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "marketplace-feed",
                "products",
                "search"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /marketplace-feed/stores",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/marketplace-feed/stores",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "marketplace-feed",
                "stores"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /marketplace-feed/stores/search",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/marketplace-feed/stores/search",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "marketplace-feed",
                "stores",
                "search"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /marketplace-feed/stores/{{storeId}}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/marketplace-feed/stores/{{storeId}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "marketplace-feed",
                "stores",
                "{{storeId}}"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /marketplace-feed/stores/{{storeId}}/products",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/marketplace-feed/stores/{{storeId}}/products",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "marketplace-feed",
                "stores",
                "{{storeId}}",
                "products"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /marketplace-feed/stores/{{storeId}}/products/search",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/marketplace-feed/stores/{{storeId}}/products/search",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "marketplace-feed",
                "stores",
                "{{storeId}}",
                "products",
                "search"
              ]
            }
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "BUDPAY ADMIN IMPORT PAYSTACK SUBACCOUNTS",
      "item": [
        {
          "name": "POST /budpay/admin/import-paystack-subaccounts",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/budpay/admin/import-paystack-subaccounts",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "budpay",
                "admin",
                "import-paystack-subaccounts"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"dryRun\": true,\n  \"limit\": 25,\n  \"storeId\": \"{{storeId}}\",\n  \"retryFailed\": false,\n  \"force\": false\n}"
            },
            "description": "Admin-only seller payout-profile import. Start with dryRun=true. force=true may replace stored BudPay identifiers."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const budpayReference = data?.reference || json?.reference || payment?.ref;",
                  "  const importResult = results[0] || (Array.isArray(data?.stores) ? data.stores[0] : null) || first;",
                  "  setIf('paymentReference', budpayReference);",
                  "  setIf('reconcileReference', budpayReference);",
                  "  setIf('budpayAccessCode', data?.access_code || json?.access_code);",
                  "  setIf('budpayCustomerId', importResult?.budpay?.customer_id || importResult?.budpay_customer_id || data?.customer?.id);",
                  "  setIf('budpayVirtualAccountId', importResult?.budpay?.virtual_account_id || importResult?.budpay_virtual_account_id);",
                  "  setIf('budpayAccountNumber', importResult?.budpay?.account_number || importResult?.budpay_account_number);",
                  "  setIf('budpayImportStatus', importResult?.result || importResult?.status || data?.status || json?.status);",
                  "  setIf('guestEmail', data?.customer?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /budpay/admin/import-paystack-subaccounts/preview",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/budpay/admin/import-paystack-subaccounts/preview?limit=100&storeId=",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "budpay",
                "admin",
                "import-paystack-subaccounts",
                "preview"
              ],
              "query": [
                {
                  "key": "limit",
                  "value": "100"
                },
                {
                  "key": "storeId",
                  "value": "{{storeId}}",
                  "disabled": true,
                  "description": "Enable for a targeted store preview."
                }
              ]
            },
            "description": "Admin-only read-only preview. Shows ready, missing-data, and already-imported stores without mutation."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const budpayReference = data?.reference || json?.reference || payment?.ref;",
                  "  const importResult = results[0] || (Array.isArray(data?.stores) ? data.stores[0] : null) || first;",
                  "  setIf('paymentReference', budpayReference);",
                  "  setIf('reconcileReference', budpayReference);",
                  "  setIf('budpayAccessCode', data?.access_code || json?.access_code);",
                  "  setIf('budpayCustomerId', importResult?.budpay?.customer_id || importResult?.budpay_customer_id || data?.customer?.id);",
                  "  setIf('budpayVirtualAccountId', importResult?.budpay?.virtual_account_id || importResult?.budpay_virtual_account_id);",
                  "  setIf('budpayAccountNumber', importResult?.budpay?.account_number || importResult?.budpay_account_number);",
                  "  setIf('budpayImportStatus', importResult?.result || importResult?.status || data?.status || json?.status);",
                  "  setIf('guestEmail', data?.customer?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /budpay/admin/import-paystack-subaccounts/status",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/budpay/admin/import-paystack-subaccounts/status?limit=100&storeId=",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "budpay",
                "admin",
                "import-paystack-subaccounts",
                "status"
              ],
              "query": [
                {
                  "key": "limit",
                  "value": "100"
                },
                {
                  "key": "storeId",
                  "value": "{{storeId}}",
                  "disabled": true,
                  "description": "Enable for one store."
                }
              ]
            },
            "description": "Admin-only BudPay seller payout-profile import status."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const budpayReference = data?.reference || json?.reference || payment?.ref;",
                  "  const importResult = results[0] || (Array.isArray(data?.stores) ? data.stores[0] : null) || first;",
                  "  setIf('paymentReference', budpayReference);",
                  "  setIf('reconcileReference', budpayReference);",
                  "  setIf('budpayAccessCode', data?.access_code || json?.access_code);",
                  "  setIf('budpayCustomerId', importResult?.budpay?.customer_id || importResult?.budpay_customer_id || data?.customer?.id);",
                  "  setIf('budpayVirtualAccountId', importResult?.budpay?.virtual_account_id || importResult?.budpay_virtual_account_id);",
                  "  setIf('budpayAccountNumber', importResult?.budpay?.account_number || importResult?.budpay_account_number);",
                  "  setIf('budpayImportStatus', importResult?.result || importResult?.status || data?.status || json?.status);",
                  "  setIf('guestEmail', data?.customer?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "BUDPAY",
      "item": [
        {
          "name": "POST /budpay/initialize",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/budpay/initialize",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "budpay",
                "initialize"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"payment_provider\": \"budpay\",\n  \"email\": \"{{userEmail}}\",\n  \"amount\": 500000,\n  \"currency\": \"NGN\",\n  \"callback_url\": \"{{callbackUrl}}\",\n  \"reference\": \"\",\n  \"metadata\": {\n    \"source\": \"postman\"\n  }\n}"
            },
            "description": "Initialize a direct BudPay Standard transaction. Amount is in kobo."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const budpayReference = data?.reference || json?.reference || payment?.ref;",
                  "  const importResult = results[0] || (Array.isArray(data?.stores) ? data.stores[0] : null) || first;",
                  "  setIf('paymentReference', budpayReference);",
                  "  setIf('reconcileReference', budpayReference);",
                  "  setIf('budpayAccessCode', data?.access_code || json?.access_code);",
                  "  setIf('budpayCustomerId', importResult?.budpay?.customer_id || importResult?.budpay_customer_id || data?.customer?.id);",
                  "  setIf('budpayVirtualAccountId', importResult?.budpay?.virtual_account_id || importResult?.budpay_virtual_account_id);",
                  "  setIf('budpayAccountNumber', importResult?.budpay?.account_number || importResult?.budpay_account_number);",
                  "  setIf('budpayImportStatus', importResult?.result || importResult?.status || data?.status || json?.status);",
                  "  setIf('guestEmail', data?.customer?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /budpay/initialize-checkout",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/budpay/initialize-checkout",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "budpay",
                "initialize-checkout"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"payment_provider\": \"budpay\",\n  \"order_payload\": {\n    \"cart\": [\n      {\n        \"id\": \"{{cartItemId}}\",\n        \"productId\": \"{{productId}}\",\n        \"variantId\": \"{{variantId}}\",\n        \"storeId\": \"{{storeId}}\",\n        \"quantity\": \"{{orderQuantity}}\"\n      }\n    ],\n    \"payment\": {\n      \"type\": \"budpay\",\n      \"callback_url\": \"{{callbackUrl}}\"\n    },\n    \"address\": {\n      \"id\": \"{{addressId}}\"\n    },\n    \"charges\": {\n      \"token\": \"{{deliveryToken}}\"\n    }\n  },\n  \"callback_url\": \"{{callbackUrl}}\"\n}"
            },
            "description": "Authenticated BudPay checkout using the same order_payload contract as Paystack."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const budpayReference = data?.reference || json?.reference || payment?.ref;",
                  "  const importResult = results[0] || (Array.isArray(data?.stores) ? data.stores[0] : null) || first;",
                  "  setIf('paymentReference', budpayReference);",
                  "  setIf('reconcileReference', budpayReference);",
                  "  setIf('budpayAccessCode', data?.access_code || json?.access_code);",
                  "  setIf('budpayCustomerId', importResult?.budpay?.customer_id || importResult?.budpay_customer_id || data?.customer?.id);",
                  "  setIf('budpayVirtualAccountId', importResult?.budpay?.virtual_account_id || importResult?.budpay_virtual_account_id);",
                  "  setIf('budpayAccountNumber', importResult?.budpay?.account_number || importResult?.budpay_account_number);",
                  "  setIf('budpayImportStatus', importResult?.result || importResult?.status || data?.status || json?.status);",
                  "  setIf('guestEmail', data?.customer?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /budpay/initialize-guest",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/budpay/initialize-guest",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "budpay",
                "initialize-guest"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"payment_provider\": \"budpay\",\n  \"guest_info\": {\n    \"email\": \"{{guestEmail}}\",\n    \"first_name\": \"Guest\",\n    \"last_name\": \"Buyer\",\n    \"phone\": \"08000000000\"\n  },\n  \"cart_items\": [\n    {\n      \"product_id\": \"{{productId}}\",\n      \"store_id\": \"{{storeId}}\",\n      \"quantity\": 1,\n      \"unit_price\": 500000\n    }\n  ],\n  \"amount\": 500000,\n  \"delivery_charge\": 0,\n  \"currency\": \"NGN\",\n  \"callback_url\": \"{{callbackUrl}}\",\n  \"order_payload\": {\n    \"guest_info\": {\n      \"email\": \"{{guestEmail}}\",\n      \"first_name\": \"Guest\",\n      \"last_name\": \"Buyer\",\n      \"phone\": \"08000000000\"\n    },\n    \"delivery_address\": {\n      \"id\": \"guest_address_001\",\n      \"full_name\": \"Guest Buyer\",\n      \"phone_no\": \"08000000000\",\n      \"full_address\": \"12 Test Street, Ikeja, Lagos\",\n      \"city\": \"Ikeja\",\n      \"state\": \"Lagos\",\n      \"state_id\": \"{{stateId}}\",\n      \"country\": \"Nigeria\",\n      \"country_id\": \"{{countryId}}\"\n    },\n    \"cart_items\": [\n      {\n        \"product_id\": \"{{productId}}\",\n        \"store_id\": \"{{storeId}}\",\n        \"product_name\": \"{{productName}}\",\n        \"quantity\": 1,\n        \"unit_price\": 500000,\n        \"total_price\": 500000\n      }\n    ],\n    \"payment\": {\n      \"payment_method\": \"budpay\",\n      \"payment_status\": \"pending\"\n    },\n    \"delivery\": {\n      \"delivery_token\": \"{{deliveryToken}}\"\n    },\n    \"order_summary\": {\n      \"subtotal\": 5000,\n      \"delivery_fee\": 0,\n      \"discount\": 0,\n      \"tax\": 0,\n      \"total\": 5000\n    }\n  }\n}"
            },
            "description": "Guest BudPay checkout using the same request contract as Paystack."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const budpayReference = data?.reference || json?.reference || payment?.ref;",
                  "  const importResult = results[0] || (Array.isArray(data?.stores) ? data.stores[0] : null) || first;",
                  "  setIf('paymentReference', budpayReference);",
                  "  setIf('reconcileReference', budpayReference);",
                  "  setIf('budpayAccessCode', data?.access_code || json?.access_code);",
                  "  setIf('budpayCustomerId', importResult?.budpay?.customer_id || importResult?.budpay_customer_id || data?.customer?.id);",
                  "  setIf('budpayVirtualAccountId', importResult?.budpay?.virtual_account_id || importResult?.budpay_virtual_account_id);",
                  "  setIf('budpayAccountNumber', importResult?.budpay?.account_number || importResult?.budpay_account_number);",
                  "  setIf('budpayImportStatus', importResult?.result || importResult?.status || data?.status || json?.status);",
                  "  setIf('guestEmail', data?.customer?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /budpay/verify",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/budpay/verify",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "budpay",
                "verify"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"reference\": \"{{paymentReference}}\"\n}"
            },
            "description": "Verify a BudPay reference and validate stored checkout amount/email."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const budpayReference = data?.reference || json?.reference || payment?.ref;",
                  "  const importResult = results[0] || (Array.isArray(data?.stores) ? data.stores[0] : null) || first;",
                  "  setIf('paymentReference', budpayReference);",
                  "  setIf('reconcileReference', budpayReference);",
                  "  setIf('budpayAccessCode', data?.access_code || json?.access_code);",
                  "  setIf('budpayCustomerId', importResult?.budpay?.customer_id || importResult?.budpay_customer_id || data?.customer?.id);",
                  "  setIf('budpayVirtualAccountId', importResult?.budpay?.virtual_account_id || importResult?.budpay_virtual_account_id);",
                  "  setIf('budpayAccountNumber', importResult?.budpay?.account_number || importResult?.budpay_account_number);",
                  "  setIf('budpayImportStatus', importResult?.result || importResult?.status || data?.status || json?.status);",
                  "  setIf('guestEmail', data?.customer?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /budpay/verify",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{authToken}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/budpay/verify?reference={{paymentReference}}",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "budpay",
                "verify"
              ],
              "query": [
                {
                  "key": "reference",
                  "value": "{{paymentReference}}"
                }
              ]
            },
            "description": "Verify a BudPay reference using a query parameter."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const budpayReference = data?.reference || json?.reference || payment?.ref;",
                  "  const importResult = results[0] || (Array.isArray(data?.stores) ? data.stores[0] : null) || first;",
                  "  setIf('paymentReference', budpayReference);",
                  "  setIf('reconcileReference', budpayReference);",
                  "  setIf('budpayAccessCode', data?.access_code || json?.access_code);",
                  "  setIf('budpayCustomerId', importResult?.budpay?.customer_id || importResult?.budpay_customer_id || data?.customer?.id);",
                  "  setIf('budpayVirtualAccountId', importResult?.budpay?.virtual_account_id || importResult?.budpay_virtual_account_id);",
                  "  setIf('budpayAccountNumber', importResult?.budpay?.account_number || importResult?.budpay_account_number);",
                  "  setIf('budpayImportStatus', importResult?.result || importResult?.status || data?.status || json?.status);",
                  "  setIf('guestEmail', data?.customer?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "POST /budpay/webhook",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "x-budpay-signature",
                "value": "{{budpayWebhookSignature}}",
                "disabled": true,
                "description": "Enable only when BUDPAY_WEBHOOK_SECRET/signature validation is configured."
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/budpay/webhook",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "budpay",
                "webhook"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\n  \"notify\": \"transaction\",\n  \"notifyType\": \"successful\",\n  \"data\": {\n    \"reference\": \"{{paymentReference}}\",\n    \"status\": \"success\",\n    \"amount\": \"500000\",\n    \"currency\": \"NGN\",\n    \"customer\": {\n      \"email\": \"{{guestEmail}}\"\n    },\n    \"metadata\": {\n      \"payment_provider\": \"budpay\"\n    }\n  }\n}"
            },
            "description": "BudPay webhook example. Production webhooks are server-verified against BudPay before order finalization."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const budpayReference = data?.reference || json?.reference || payment?.ref;",
                  "  const importResult = results[0] || (Array.isArray(data?.stores) ? data.stores[0] : null) || first;",
                  "  setIf('paymentReference', budpayReference);",
                  "  setIf('reconcileReference', budpayReference);",
                  "  setIf('budpayAccessCode', data?.access_code || json?.access_code);",
                  "  setIf('budpayCustomerId', importResult?.budpay?.customer_id || importResult?.budpay_customer_id || data?.customer?.id);",
                  "  setIf('budpayVirtualAccountId', importResult?.budpay?.virtual_account_id || importResult?.budpay_virtual_account_id);",
                  "  setIf('budpayAccountNumber', importResult?.budpay?.account_number || importResult?.budpay_account_number);",
                  "  setIf('budpayImportStatus', importResult?.result || importResult?.status || data?.status || json?.status);",
                  "  setIf('guestEmail', data?.customer?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "GET /budpay/public-key",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/budpay/public-key",
              "host": [
                "{{baseUrl}}"
              ],
              "path": [
                "budpay",
                "public-key"
              ]
            },
            "description": "Return the configured BudPay public key when frontend use requires it."
          },
          "response": [],
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "let json = null;",
                  "try {",
                  "  json = pm.response.json();",
                  "} catch (error) {",
                  "  console.log('Response is not valid JSON', error);",
                  "}",
                  "if (json) {",
                  "  const setIf = (key, value) => {",
                  "    if (value !== undefined && value !== null && value !== '') {",
                  "      const normalized = String(value);",
                  "      pm.collectionVariables.set(key, normalized);",
                  "      if (pm.environment) {",
                  "        pm.environment.set(key, normalized);",
                  "      }",
                  "    }",
                  "  };",
                  "  const unsetVar = (key) => {",
                  "    pm.collectionVariables.unset(key);",
                  "    if (pm.environment) {",
                  "      pm.environment.unset(key);",
                  "    }",
                  "  };",
                  "  const decodeJwtPayload = (token) => {",
                  "    try {",
                  "      if (!token || String(token).split('.').length < 2) return null;",
                  "      let payload = String(token).split('.')[1].replace(/-/g, '+').replace(/_/g, '/');",
                  "      while (payload.length % 4) payload += '=';",
                  "      return JSON.parse(atob(payload));",
                  "    } catch (error) {",
                  "      console.log('Unable to decode JWT payload', error);",
                  "      return null;",
                  "    }",
                  "  };",
                  "  const data = json?.data ?? json;",
                  "  const rows = Array.isArray(data?.rows) ? data.rows : [];",
                  "  const results = Array.isArray(data?.results) ? data.results : [];",
                  "  const nestedList = Array.isArray(data?.data) ? data.data : [];",
                  "  const list = Array.isArray(data) ? data : (rows.length ? rows : (results.length ? results : nestedList));",
                  "  const first = list[0] || data?.newOrder || data?.order || data?.store || data?.product || data?.user || data || {};",
                  "  const order = first?.newOrder || first?.order || data?.newOrder || data?.order || null;",
                  "  const payment = order?.payment || order?.orderPayment || first?.payment || data?.payment || data?.orderPayment || null;",
                  "  const store = first?.store || first?.storeDetails || data?.store || data?.storeDetails || null;",
                  "  const user = data?.user || first?.user || first;",
                  "  const product = first?.product || data?.product || first;",
                  "  const address = first?.address || data?.address || first;",
                  "  const category = first?.category || data?.category || first;",
                  "  const subCategory = first?.subCategory || first?.subcategory || data?.subCategory || data?.subcategory || first;",
                  "  const enquiry = data?.enquiry || first?.enquiry || first;",
                  "  const firstVariant = Array.isArray(product?.variants) ? product.variants[0] : (Array.isArray(first?.variants) ? first.variants[0] : null);",
                  "  const reconcile = results.find((entry) => entry?.action === 'missing' || entry?.action === 'failed' || entry?.action === 'skipped') || results[0] || null;",
                  "  const budpayReference = data?.reference || json?.reference || payment?.ref;",
                  "  const importResult = results[0] || (Array.isArray(data?.stores) ? data.stores[0] : null) || first;",
                  "  setIf('paymentReference', budpayReference);",
                  "  setIf('reconcileReference', budpayReference);",
                  "  setIf('budpayAccessCode', data?.access_code || json?.access_code);",
                  "  setIf('budpayCustomerId', importResult?.budpay?.customer_id || importResult?.budpay_customer_id || data?.customer?.id);",
                  "  setIf('budpayVirtualAccountId', importResult?.budpay?.virtual_account_id || importResult?.budpay_virtual_account_id);",
                  "  setIf('budpayAccountNumber', importResult?.budpay?.account_number || importResult?.budpay_account_number);",
                  "  setIf('budpayImportStatus', importResult?.result || importResult?.status || data?.status || json?.status);",
                  "  setIf('guestEmail', data?.customer?.email || first?.guest_email);",
                  "}"
                ]
              }
            }
          ]
        }
      ]
    }
  ],
  "variable": [
    {
      "key": "baseUrl",
      "value": "http://localhost:8000",
      "type": "string",
      "enabled": true
    },
    {
      "key": "authToken",
      "value": "",
      "type": "string"
    },
    {
      "id": "refreshToken",
      "key": "refreshToken",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "userId",
      "key": "userId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "managedUserId",
      "key": "managedUserId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "userEmail",
      "key": "userEmail",
      "type": "string",
      "value": "olagiddz@gmail.com",
      "enabled": true
    },
    {
      "id": "activeRole",
      "key": "activeRole",
      "type": "string",
      "value": "user",
      "enabled": true
    },
    {
      "id": "storeId",
      "key": "storeId",
      "type": "number",
      "value": "4548",
      "enabled": true
    },
    {
      "id": "orderId",
      "key": "orderId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "productId",
      "key": "productId",
      "type": "number",
      "value": "13628",
      "enabled": true
    },
    {
      "id": "variantId",
      "key": "variantId",
      "type": "number",
      "value": "null",
      "enabled": true
    },
    {
      "id": "addressId",
      "key": "addressId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "deliveryToken",
      "key": "deliveryToken",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "paymentReference",
      "key": "paymentReference",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "reconcileReference",
      "key": "reconcileReference",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "guestEmail",
      "key": "guestEmail",
      "type": "string",
      "value": "olagiddz@gmail.com",
      "enabled": true
    },
    {
      "id": "guestCheckoutId",
      "key": "guestCheckoutId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "paystackStatus",
      "key": "paystackStatus",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "paystackCustomerEmail",
      "key": "paystackCustomerEmail",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "paystackAuthUrl",
      "key": "paystackAuthUrl",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "paystackAmountKobo",
      "key": "paystackAmountKobo",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "migrationStatus",
      "key": "migrationStatus",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "migrationResult",
      "key": "migrationResult",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "paystackMigrationError",
      "key": "paystackMigrationError",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "enquiryId",
      "key": "enquiryId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "paystackSignature",
      "key": "paystackSignature",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "orderPublicId",
      "key": "orderPublicId",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "categoryId",
      "key": "categoryId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "subCategoryId",
      "key": "subCategoryId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "storeSlug",
      "key": "storeSlug",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "productPid",
      "key": "productPid",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "phone",
      "key": "phone",
      "type": "string",
      "value": "2348012345678",
      "enabled": true
    },
    {
      "id": "slug",
      "key": "slug",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "token",
      "key": "token",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "invoiceId",
      "key": "invoiceId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "invoiceToken",
      "key": "invoiceToken",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "countryId",
      "key": "countryId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "stateId",
      "key": "stateId",
      "type": "number",
      "value": "25",
      "enabled": true
    },
    {
      "id": "bannerId",
      "key": "bannerId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "offerId",
      "key": "offerId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "notificationId",
      "key": "notificationId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "bankAccountId",
      "key": "bankAccountId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "companyId",
      "key": "companyId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "driverId",
      "key": "driverId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "position",
      "key": "position",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "reference",
      "key": "reference",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "bankCode",
      "key": "bankCode",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "type",
      "key": "type",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "limit",
      "key": "limit",
      "type": "number",
      "value": "10",
      "enabled": true
    },
    {
      "id": "id",
      "key": "id",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "buyerEmail",
      "key": "buyerEmail",
      "type": "string",
      "value": "olagiddz@gmail.com",
      "enabled": true
    },
    {
      "id": "adminEmail",
      "key": "adminEmail",
      "type": "string",
      "value": "admin@example.com",
      "enabled": true
    },
    {
      "id": "adminResetToken",
      "key": "adminResetToken",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "adminNewPassword",
      "key": "adminNewPassword",
      "type": "string",
      "value": "NewStrongPassword123!",
      "enabled": true
    },
    {
      "key": "oldPassword",
      "value": "OldPassword123!",
      "type": "string"
    },
    {
      "key": "newPassword",
      "value": "NewPassword123!",
      "type": "string"
    },
    {
      "key": "resetToken",
      "value": "",
      "type": "string"
    },
    {
      "id": "password",
      "key": "password",
      "value": "Options123#",
      "type": "string",
      "enabled": true
    },
    {
      "id": "callbackUrl",
      "key": "callbackUrl",
      "value": "http://localhost:3000/payment/callback",
      "type": "string",
      "enabled": true
    },
    {
      "id": "cartItemId",
      "key": "cartItemId",
      "value": "1",
      "type": "number",
      "enabled": true
    },
    {
      "id": "productName",
      "key": "productName",
      "value": "Sample product",
      "type": "string",
      "enabled": true
    },
    {
      "id": "productPrice",
      "key": "productPrice",
      "value": "5000",
      "type": "number",
      "enabled": true
    },
    {
      "id": "productWeight",
      "key": "productWeight",
      "value": "1",
      "type": "number",
      "enabled": true
    },
    {
      "id": "orderQuantity",
      "key": "orderQuantity",
      "value": "1",
      "type": "number",
      "enabled": true
    },
    {
      "id": "paystackAccessCode",
      "key": "paystackAccessCode",
      "value": "",
      "type": "string",
      "enabled": true
    },
    {
      "id": "productImage",
      "key": "productImage",
      "type": "string",
      "value": "https://example.com/product.jpg",
      "enabled": true
    },
    {
      "id": "deliveryCharge",
      "key": "deliveryCharge",
      "type": "number",
      "value": "0",
      "enabled": true
    },
    {
      "id": "deliveryDiscount",
      "key": "deliveryDiscount",
      "type": "number",
      "value": "0",
      "enabled": true
    },
    {
      "id": "deliveryTokenAddressId",
      "key": "deliveryTokenAddressId",
      "type": "number",
      "value": "",
      "enabled": true
    },
    {
      "id": "paymentProvider",
      "key": "paymentProvider",
      "type": "string",
      "value": "paystack",
      "enabled": true
    },
    {
      "id": "budpayAccessCode",
      "key": "budpayAccessCode",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "budpayCustomerId",
      "key": "budpayCustomerId",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "budpayVirtualAccountId",
      "key": "budpayVirtualAccountId",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "budpayAccountNumber",
      "key": "budpayAccountNumber",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "budpayImportStatus",
      "key": "budpayImportStatus",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "budpayWebhookSignature",
      "key": "budpayWebhookSignature",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "boosterPlanId",
      "key": "boosterPlanId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "boosterConfigId",
      "key": "boosterConfigId",
      "type": "number",
      "value": "1",
      "enabled": true
    },
    {
      "id": "boosterReference",
      "key": "boosterReference",
      "type": "string",
      "value": "",
      "enabled": true
    },
    {
      "id": "boosterTier",
      "key": "boosterTier",
      "type": "string",
      "value": "gold",
      "enabled": true
    },
    {
      "id": "boosterDurationDays",
      "key": "boosterDurationDays",
      "type": "number",
      "value": "30",
      "enabled": true
    },
    {
      "id": "managedRole",
      "key": "managedRole",
      "type": "string",
      "value": "admin",
      "enabled": true
    },
    {
      "id": "managedStatus",
      "key": "managedStatus",
      "type": "boolean",
      "value": "true",
      "enabled": true
    }
  ]
}